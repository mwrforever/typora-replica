# 08 主题 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 08 主题模块全链路（spec T1~T8，18 条 AC）——Rust 主题目录服务（命名规则校验、目录扫描、内置主题预置、热刷新监视、打开主题目录、DevTools 开关）+ 前端主题链路（themeStore 状态机、4 层 CSS 注入服务、明暗分离与系统色系联动、热刷新重挂、Shift+F12 接线）+ 内置原创亮/暗主题与 Typora 变量映射层 + E2E 核定链路。

**Architecture:** Rust 侧新增 `io/themes.rs`（纯函数核心显式收 `dir` 参数供单测，命令层薄壳经 `app.path().app_data_dir()/themes` 解析目录）与顶层 `devtools.rs`（cfg 门控 DevTools）；复用 07 的 `allow_asset_directory` 做运行时 asset 授权、复用 `watch.rs` 三件套（`watch_dir_inner` + `flush_loop` + AppState 槽位）承载主题监视。前端 `src/features/theme/` 四层——IPC 收敛（services/theme-io）、注入服务（theme-css 只操作 `document.head` 的固定 id link，绝不触碰编辑器 DOM）、状态机（themeStore：明暗分离设置镜像 + 系统色系联动 + 热刷新防抖）、装配（register + App.vue）。主题 CSS 以 asset 协议 `<link>` 注入（命名规则保证 URL 安全），CSS 4 层顺序 = 基础(静态)→主题 link→base.user.css→{theme}.user.css。

**Tech Stack:** Tauri 2.11.5（protocol-asset）/ notify 8.2 / tauri-plugin-opener 2.5.4（Rust 侧 `OpenerExt::open_path`）/ regex（既有依赖）/ Milkdown Crepe 7.22.1（仅消费 22 个 `--crepe-*` 变量层，无 schema/插件改动）/ Vue 3.5 + Pinia 4 / Vitest 4 + jsdom / WebdriverIO 9。

## Global Constraints

- 分支 `feat/08-theme` 自 `test`（2b77db6）切出；main 冻结，一切合入 `test`
- 注释/日志全中文（描述业务意图与"为什么"）；Rust 诊断沿用 `eprintln!` + `[MarkWell]` 前缀；日志禁敏感信息（完整路径不落错误消息）
- 全部文本文件 UTF-8 无 BOM、行尾 LF；文件末尾保留一个换行
- git 提交：`type(scope): 中文描述`，type 限 feat/fix/test/docs/chore 等；一条提交只做一件事；**测试与实现同次提交**
- TDD 铁律：每任务先写失败测试（验证失败原因正确）→ 最小实现转绿 → REFACTOR；TDD 证据（RED 失败输出 + GREEN 通过输出）写入任务报告
- Rust 门禁：库代码禁 `panic!`/`unwrap()`/`expect()`（测试与"确定不可达处须注释理由"除外）；整库禁 `unsafe`；`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 与 `cargo fmt --check` 全绿；错误沿用既有 `Result<T, String>` 中文消息形态（Cargo.toml 无 thiserror，既有风格优先——宪法 A.1.1）
- TS：`strict` 家族禁逐项关闭；禁 `any`；null/undefined 默认 `undefined`；`import type` 修饰类型导入
- Milkdown/ProseMirror 边界（宪法 B.2.5）：一切 ProseMirror API 经 `@milkdown/kit/prose/*`；禁直装 `prosemirror-*`；**禁直改编辑器 DOM**（08 只操作 `document.head` 内固定 id 的 link 与 `document.documentElement` 根类）；schema 扩展按 id 原位 upsert（08 无 schema 扩展）；自定义编辑器插件三段式（08 无编辑器插件）；Crepe 为唯一骨架
- IPC 契约（宪法 A.3）：命令名 snake_case 全局唯一、`generate_handler!` 只允许追加；参数/返回 serde 契约 + DTO `rename_all = "camelCase"`；前端 interface 按**序列化后 JSON 形状**建模、请求/响应分离不复用；错误 `Result<T, String>` 中文；命令形参 > 3 须参数 struct（本模块命令形参均 ≤ 2，不触发）；大负载禁 JSON 返回（主题 CSS 一律经 asset 协议 link，不经 IPC 传字节；事件流走 `tauri::ipc::Channel`）
- asset 安全（宪法 B.4.3 + spec §3）：assetProtocol scope 保持 `[]` 空起步，运行时经既有 `allow_asset_directory(themesDir)` 递归授权（07 先例）；CSP 仅增补 `style-src`/`font-src` 的 `asset: http://asset.localhost`（spec §3 明文要求；img-src 已具备）；禁止 `dangerousDisableAssetCspModification`
- 覆盖率（宪法 A.6.2）：主题前端核心域 4 文件 100% glob 同步进 `vite.config.ts`（Task 16 专项全量验证）；覆盖率门禁必须走 `npm run test:coverage`，禁止裸 `vitest run` 冒充
- README 同步（宪法 B.5.6）：README.md 属 gitignored 本地文档（.gitignore:59，1a44a76 用户裁决），Task 17 更新内容、不入库
- 参数对象化与返回业务对象（宪法 A.7）：导出函数形参 > 3 用 interface 整体传参；返回具名 interface；职责隔离——`ThemeMeta` ≠ `ThemeSettings` ≠ store state ≠ 注入入参，theme-io 独立 `ThemeIoError`（不复用 file-io 的 `FileIoError`）
- 内存红线（宪法 C.6.2）：vitest `maxWorkers: 4` + `--max-old-space-size=2048` 禁还原；运行测试/推送前甄别残留 vitest 进程（严禁误杀其他项目 node 进程）
- 平台锁定 Windows（x86_64-pc-windows-msvc）；E2E 前先 `cargo build`（application 指 `src-tauri/target/debug/typora-replica.exe`）
- clean-room（宪法 B.5.5）：内置主题为原创 CSS，禁止解包/反编译/复制 Typora 主题文件；官方 6 主题仅作风拍参考
- 单一事实源：发现 spec 缺陷先升级用户回改 spec（宪法 B.2.2），禁止边改 spec 边实现
- 死代码零容忍（全局 §四）：预留接口以 `// TODO(<feature>): <扩展说明，计划于 X 版本引入>` 标注（本计划仅 `openThemeFolder` 一处，消费方为 10/12）

## 实施前已锁定裁决（按推荐方案执行，PR 把关时可否决）

| # | 裁决 | 内容与理由 |
| --- | --- | --- |
| D-1 | 内置主题落位 = Rust 资产 + 启动预置 | 两套原创 CSS 存 `src-tauri/assets/themes/`，`include_str!` 入二进制，setup 钩子预置到 `app_data_dir/themes/`（仅缺失才写，用户改内置主题不覆盖）。理由：内置与用户主题走**单一加载路径**（asset 协议 link）、主题列表天然统一（`list_themes` 一并扫出）、用户可改内置主题（Typora 惯例）、热刷新一致生效、打包零配置 |
| D-2 | open_theme_folder 用 opener 插件 Rust API `open_path` | 打开主题目录本身（Explorer 列出 css 文件），对齐 Typora「打开主题目录」语义；`reveal_item_in_dir` 会落在父级选中目录，语义偏差。Rust 内部调用不经 opener 插件 IPC 权限面，仅需自有命令权限 `allow-open-theme-folder`（已核验 tauri-plugin-opener 2.5.4 `OpenerExt::open_path(path, None::<&str>)`，lib.rs:116-129） |
| D-3 | 明暗源 = 系统跟随，不调用 `setTheme` 钉死 | 前端 `matchMedia('(prefers-color-scheme: dark)')` 监听 + 初始求值（color-scheme 模块）。钉死窗口主题会冻结 WebView2 PreferredColorScheme，反向破坏主题内 media query 自适应（AC-T5-2）。WebView2 默认色系跟随系统；若实测不成立，补救 = 后续接 `onThemeChanged` 双通道（登记为 P5 手动验证项与 PR 披露，本期不做双源竞态设计） |
| D-4 | 主题 CSS 作用域约定 = 双选择器 `.milkdown, .markwell-dark .milkdown { }` | crepe-overrides.css 暗色段特异性 (0,2,0) 会压过单 `.milkdown` 主题段 (0,1,0)；双选择器使主题段与暗色默认段打平后凭 link 注入顺序取胜（亮色段同理）。内置主题按此编写，README 文档化约定 |
| D-5 | notify 8.2 静默 unwatch 风险 = 接受 + 披露 | 宪法 A.5.4 指出 Windows 缓冲溢出静默停止监听。主题目录为低频小目录（用户手工放 css），溢出概率极低；完整存活检测/重建兜底不做（简单优先），登记 PR 披露；热刷新订阅失败降级 = 重启可见（即官方基线行为） |
| D-6 | 命令层测试策略 = 纯函数核心 100% + wire 契约 E2E 实证 | 已核验 mock 运行时 `identifier` 为空串，`app.path().app_data_dir()` 解析到**真实 %APPDATA%**（tauri-2.11.5 src/path/desktop.rs:247-250）——`list_themes`/`watch_themes`/`open_theme_folder` 在 mock 下直呼会触达真实用户数据，禁止。故核心函数显式收 `dir: &Path` 100% 单测；命令薄壳不 mock 直呼（偏离 watch.rs:302-337 先例的理由即此，watch.rs 先例命令收显式 path 参数所以可直呼）；wire 正确性由 E2E（Task 18）实证。DTO 序列化形态（camelCase）以 serde_json 单测钉住 |
| D-7 | 热刷新防缓存 = link href 追加 `?t=<毫秒时间戳>` 重挂同 id | 调研拍板方案；`applyThemeCss` 幂等 upsert，时间戳变化即触发 WebView 重新拉取 |
| D-8 | 大小写敏感 = Rust `read_dir` 文件名**精确字符串匹配** | Windows 文件系统大小写不敏感，`Path::exists()` 会误命中大小写不同的 `{theme}.user.css`（AC-T6-3）；扫描一次收集文件名列表后精确比对 |
| D-9 | AC-T5-2（主题内 media query 自适应）= 注入链路零改写 + 披露 | 主题 CSS 原样经 link 加载，media query 由 WebView2 按系统色系原生求值（D-3 前提）；jsdom/WebDriver 均无法模拟系统色系切换，本期验证 = Vitest 钉「注入不改写主题内容」+ PR 披露 |
| D-10 | 主题 CSS 的 URL 安全性由命名规则保证 | 文件名仅小写字母+连字符（Rust 校验），拼接 asset URL 无需 encode；字体等资源文件名不在校验面（由主题作者负责，README 注明建议小写连字符） |

## 依赖面核查（2026-09-05 只读核验）

### 交付前已核定结论（直接采用）

1. `read_file` 命令存在：`src-tauri/src/io/commands.rs:120-133`（`pub fn read_file(path: String) -> Result<ReadResultDto, String>`，DTO camelCase）；`lib.rs:37` 注册（共 20 命令）；前端封装 `src/services/file-io.ts:90`（invokeOrThrow 形态）。→ 08 **不新增** CSS 读取命令（扫描在 Rust 侧完成，前端不经 IPC 逐文件读 CSS）。
2. `src/services/settings.ts` 存在但无 theme 字段（12-149 行：类型 `AppSettings`/`DEFAULT_SETTINGS`/`loadSettings`/`updateSettings`）；`updateSettings` patch 为 `Partial<Omit<AppSettings, "launch" | "outline" | "image">>` & 分组 Partial——08 增 theme 组须同步扩 Omit 联合、DEFAULT_SETTINGS、loadSettings 逐键回落、updateSettings 合并与写回四触点；settings.ts 在 coverage 100% 名单，扩展后 spec 须补全分支。
3. **10 设置模块未落地**：`src/features/` 无 settings 目录（现有 8 域：document/editor/file-tree/image/open-quickly/outline/search/tabs）；Pinia 4 store（search/file-tree/tabs/outline）。
4. **12 菜单装配未落地**：无原生菜单代码，现有 menu 均为右键菜单。→ AC-T1-1「Themes 菜单列出」、AC-T2-1「Appearance 分区按钮」、AC-T5-1「设置入口」依赖 10/12 UI，本模块只交付 store/服务层数据与动作，验证层级降级（见下方降级清单），**须列入 PR 披露清单**。
5. `.markwell-dark`：CSS 消费端已就绪（`src/styles/crepe-overrides.css:29-48` 暗色变量块、142/145/154/169 各暗色段）；**运行时激活 JS 全项目不存在（08 自建）**；`src/features/image/zoom-render.spec.ts:156-165` 有正则钉桩守护暗色段行首形态——**改 crepe-overrides.css 不可破坏该钉桩**（本计划不修改该文件）。tokens.css 仅字体令牌无颜色令牌。
6. `tauri.conf.json`：CSP 现状 `style-src 'self' 'unsafe-inline'`；`font-src 'self' data:`；`img-src 'self' data: blob: asset: http://asset.localhost` → 08 须放行 style-src/font-src 的 `asset: http://asset.localhost`（主题 CSS link 与 fonts/ 字体经 asset 协议）。`assetProtocol enable:true scope:[]`（空起步，运行时按需授权先例 = 07 `allow_asset_directory`）。`withGlobalTauri` 键不存在（保持）。identifier `com.markwell.app`。
7. `capabilities/default.json` 唯一 capability、24 权限白名单；新命令须追加 `allow-list-themes` / `allow-open-theme-folder` / `allow-watch-themes` / `allow-toggle-devtools`；permissions/ 仅 autogenerated（DO NOT EDIT）。
8. `watch.rs` 可复用三件套：`watch_dir_inner` 纯函数（23-63 行）+ `flush_loop` 100ms 合并（73-93 行，`pub(super)`，io 模块树内兄弟模块可复用——search.rs 先例）+ `watch_dir`/`unwatch_dir` 命令层（101-152 行）；`AppState` 定义在 `lib.rs:5-11`。**实测 AppState 构造点 = lib.rs:23（生产）+ watch.rs:305 与 search.rs:668（mock 测试）共三处**，增字段须同步。
9. `vite.config.ts` coverage thresholds：全局四项 ≥80% + 20 个 100% glob（53-202 行）；08 主题核心域新增 glob 见 Task 16。
10. CSS 注入服务不存在（`main.ts:5-6` 仅静态引入 tokens.css 与 crepe-overrides.css）；无前端监视依赖；Cargo.toml 无 thiserror → 错误沿用 `Result<T, String>` 中文。
11. `lib.rs` Builder 链无 setup 钩子（plugin×3 → manage → invoke_handler → run）→ 08 新增 setup 钩子预置内置主题（宪法 A.5.7：初始化集中 setup，钩子内禁 panic）。
12. e2e/specs/*.e2e.ts 命名 7 用例；wdio capability `application` 指 `src-tauri/target/debug/typora-replica.exe`；Rust 变更后须先 `cargo build` 再跑 E2E（e2e/README）。
13. Crepe 22 变量（lib/theme/crepe/style.css:1-23）：17 色（--crepe-color-background/-on-background/-surface/-surface-low/-on-surface/-on-surface-variant/-outline/-primary/-secondary/-on-secondary/-inverse/-on-inverse/-inline-code/-error/-hover/-selected/-inline-area）+ --crepe-base-font-size + 3 字体（--crepe-font-title/-default/-code）+ 2 阴影（--crepe-shadow-1/-2）；主题文件以 `.milkdown {` 作用域；crepe-overrides.css 亮色已覆盖 20 变量、暗色 17 变量、base-font-size 与 shadow 未覆盖。
14. Rust 命令测试先例：`watch.rs:302-337`（tauri::test::mock_app + app.manage(AppState) + 直呼命令 + tauri::ipc::Channel 占位）；`images.rs:545-556`（asset scope is_allowed 断言）。settings.spec.ts mock 先例：`vi.hoisted` memory Map（vi.mock 工厂 TDZ 约束）。`src/test/setup.ts:37-48` 已有 matchMedia 桩（恒 false）——color-scheme 测试逐用例覆盖 `window.matchMedia`。
15. Typora 变量映射层：官方 15 常用变量（--bg-color/--text-color/--primary-color/--side-bar-bg-color 等，官方明言可能变化）→ 映射到 `--crepe-*` 兜底；DOM 选择器（#write 等）不兼容属预期（披露）。

### 本次计划补充核验（2026-09-05，沙箱源码实证）

16. mock 运行时 `app_data_dir()` = `dirs::data_dir().join(config.identifier)`，而 `tauri::test::mock_context` 的 identifier 为**空串默认值**（tauri-2.11.5 src/test/mod.rs:109-135）→ 解析至真实 %APPDATA%——支撑 D-6（命令层禁 mock 直呼路径解析命令）。
17. tauri-plugin-opener 2.5.4：`OpenerExt` trait 提供 `app.opener().open_path(path, None::<&str>)`（src/lib.rs:116-129）与独立函数 `reveal_item_in_dir`；Rust 内部调用不经插件 IPC 权限（权限仅门控插件自有命令）。
18. tauri 2.11.5 的 `open_devtools`/`close_devtools`/`is_devtools_open` 带 `#[cfg(any(debug_assertions, feature = "devtools"))]` 门控（src/webview/webview_window.rs:2438/2471）→ debug 构建默认可用，release 须映射 `tauri/devtools` feature（Task 14）。
19. README.md 与 docs/ 均被 .gitignore（58-60 行，1a44a76「非代码与宪法文档退出版本库」用户裁决）→ 计划与 README 更新均为本地文档产物，不随 git 提交。
20. `ep` 无关；`package.json` 已含 `@tauri-apps/plugin-opener ^2`（JS 侧不新增依赖）；regex 已是 Rust 依赖。

### 10/12 未落地 → AC 验证层级降级清单（PR 披露必备）

| AC | 依赖的未落地 UI | 本模块验证层级 | PR 披露要点 |
| --- | --- | --- | --- |
| AC-T1-1 | Themes 菜单展示（12） | Rust 扫描单测（Task 2/3）+ Vitest `store.themes`（Task 8）| 菜单 UI 呈现归 12，数据链路已验 |
| AC-T2-1 | Appearance 分区按钮（10/12） | Rust 命令实现（Task 4）+ Vitest 服务封装契约（Task 6） | 触发按钮归 10/12，`open_theme_folder` 命令已就位（预留 TODO 标注） |
| AC-T3-1 | 「出现在 Themes 菜单」的菜单段（12） | Rust 监视单测（Task 9）+ Vitest 事件→`store.themes` 更新（Task 10） | 数据刷新链路已验，菜单呈现归 12 |
| AC-T5-1 | 明暗主题设置入口（10） | Vitest matchMedia 联动链路（Task 12/13） | 真实系统切换 E2E 不可模拟（D-9）；设置入口归 10 |
| AC-T7-1 | View 菜单项（12） | Vitest Shift+F12 接线（Task 14）+ Rust cfg 双分支编译验证 | DevTools 开合的可视断言无自动化，debug 构建手动验证 |
| AC-T8-2 | 字号偏好设置（10） | Vitest rem 钉桩（Task 3/15） | 偏好联动链路待 10 落地后 E2E 复验 |

## 文件结构

```text
src-tauri/
├── assets/themes/                      # 内置原创主题（clean-room，include_str! 入二进制）
│   ├── markwell-light.css              # 亮色（Task 3）
│   └── markwell-dark.css               # 暗色（Task 3）
├── src/
│   ├── lib.rs                          # AppState 增 theme_watcher 槽位 + setup 预置 + 命令注册追加
│   ├── devtools.rs                     # 新增：toggle_devtools（cfg 门控，Task 14）
│   └── io/
│       ├── mod.rs                      # + pub mod themes;
│       └── themes.rs                   # 新增：命名/标签纯函数 + scan_themes + 预置 + 三命令
├── capabilities/default.json           # 追加 4 个 allow-* 权限
├── tauri.conf.json                     # CSP style-src/font-src 放行 asset:
└── Cargo.toml                          # + [features] devtools = ["tauri/devtools"]
src/
├── main.ts                             # + 静态引入 typora-var-bridge.css（Task 11）
├── App.vue                             # + registerThemeFeature 装配（Task 8）/cleanup（Task 13）
├── styles/typora-var-bridge.css        # 新增：Typora 变量映射兜底层（Task 11）
├── services/
│   ├── theme-io.ts                     # 新增：主题域 IPC 收敛（Task 6）
│   ├── theme-io.spec.ts
│   ├── settings.ts                     # + theme 组（Task 5）
│   └── settings.spec.ts
└── features/theme/                     # 新增功能域
    ├── register.ts                     # 装配入口（Task 8 建，Task 14 扩）
    ├── theme-store.ts                  # 状态机（Task 8 建，Task 10/13 扩）
    ├── theme-store.spec.ts
    ├── theme-css.ts                    # 4 层注入服务（Task 7）
    ├── theme-css.spec.ts
    ├── color-scheme.ts                 # 系统色系监视（Task 12）
    ├── color-scheme.spec.ts
    ├── devtools.ts                     # Shift+F12 接线（Task 14）
    ├── devtools.spec.ts
    └── builtin-theme.spec.ts           # 内置主题钉桩（Task 15）
e2e/specs/theme.e2e.ts                  # 新增 E2E（Task 18）
vite.config.ts                          # + 主题核心域 4 glob（Task 16）
README.md                               # 本地文档更新（Task 17，不入库）
```

## 18 条 AC → 任务映射表

> **主控勘误注（2026-09-05 P1 门禁核验）**：spec §7 声称「共 18 条」，但 spec §2 的 AC 表实际为 **16 条**（T1:2 + T2:1 + T3:2 + T4:3 + T5:2 + T6:3 + T7:1 + T8:2 = 16）。本计划与执行文档沿用「18 条」措辞处，一律以 AC 表实际 16 条为准（下表 16 行全覆盖、无遗漏）。spec §7 计数勘误建议已列入 PR 披露，改 spec 待用户同意（宪法 B.2.2）。

| AC | 验收标准（摘要） | 承接任务 | 测试层级 |
| --- | --- | --- | --- |
| AC-T1-1 | 主题目录含 3 个 .css，Themes 菜单列出 3 个（含内置） | Task 2（扫描过滤/排序单测）、Task 3（内置预置）、Task 8（store.themes）、Task 18（E2E 目录与注入实证） | Rust 单测 + Vitest + E2E（菜单 UI 段降级 Vitest，披露） |
| AC-T1-2 | 选择某主题，界面即时切换该主题样式 | Task 7（link 重挂单测）、Task 8（selectTheme→href 变化）、Task 18（启动默认主题真实生效） | Vitest + E2E |
| AC-T2-1 | 点击 Open Theme Folder，资源管理器打开主题目录 | Task 4（open_theme_folder 命令）、Task 6（服务封装契约） | Rust 实现 + Vitest（按钮触发降级，披露） |
| AC-T3-1 | 复制新主题 css，热刷新后出现在菜单（无需重启） | Task 9（watch_themes 单测）、Task 10（事件→列表刷新） | Rust 单测 + Vitest（菜单呈现段降级，披露） |
| AC-T3-2 | 修改已激活主题 css，界面即时刷新 | Task 10（防抖重挂）、Task 18（E2E href ?t= 变化 + 变量更新） | Vitest + E2E |
| AC-T4-1 | my-first-theme.css → 标签 "My First Theme" | Task 1（标签转换单测） | Rust 单测 |
| AC-T4-2 | theme2.css（含数字）不出现在菜单 | Task 1（数字拒绝）、Task 2（扫描过滤） | Rust 单测 |
| AC-T4-3 | MyTheme.css（大写）不出现在菜单 | Task 1（大写拒绝）、Task 2（扫描过滤） | Rust 单测 |
| AC-T5-1 | 分别设置亮/暗主题，切换系统明暗，对应主题自动应用 | Task 12（matchMedia 监视）、Task 13（联动注入 + 根类切换） | Vitest（真实系统切换不可 E2E，披露） |
| AC-T5-2 | 主题内 media query 自适应，明暗切换时生效 | Task 7（注入仅重挂 link、零改写主题内容——AC-T5-2 注入侧钉桩）+ 裁决 D-3/D-9 | Vitest + 设计保证（披露） |
| AC-T6-1 | base.user.css 存在，样式对所有主题生效 | Task 2（hasBaseUserCss 扫描）、Task 7（第 3 层挂载与顺序） | Rust 单测 + Vitest |
| AC-T6-2 | {theme}.user.css 存在，仅该主题生效 | Task 2（hasUserCss 扫描）、Task 7（第 4 层挂载/随主题移除） | Rust 单测 + Vitest |
| AC-T6-3 | 文件名大小写不匹配，不生效（大小写敏感） | Task 2（read_dir 精确字符串匹配单测，裁决 D-8） | Rust 单测 |
| AC-T7-1 | debug 构建按 Shift+F12，DevTools 开合切换 | Task 14（Rust toggle_devtools + 前端快捷键接线） | Vitest + Rust cfg 编译验证（可视断言手动，披露） |
| AC-T8-1 | 主题引用 ./fonts/x.woff2，字体正确加载 | Task 15（href 目录基准断言 + 07 授权递归复用 + README 约定） | Vitest 组合证据（披露） |
| AC-T8-2 | 内置主题 rem 字号，调整字号偏好全文跟随 | Task 3（内置 CSS rem）、Task 15（rem/无 px 钉桩） | Vitest（偏好联动待 10，披露） |

spec §1.1 范围 11 项承接核对：①T1→Task 1-8/18；②T2→Task 4/6；③T3→Task 9/10/18；④T4→Task 1/2；⑤T5→Task 12/13；⑥T6→Task 2/7；⑦T7→Task 14；⑧T8→Task 3/15；⑨接口面（themeStore/list_themes/watch_themes/注入服务）→Task 4/6/7/8/9；⑩非功能（22 变量原创+映射层+scope/CSP）→Task 3/4/11；⑪测试策略+coverage glob+README→Task 15/16/17/18 及各任务测试。无遗漏。

## 批次编列表（SDD P2 登记 ledger 用；批类型 = 批内最高复杂度）

| 批次 | spec 阶段 | 任务集 | 类型 | 任务数 | BASE（开批时 `git rev-parse HEAD` 回填） |
| --- | --- | --- | --- | --- | --- |
| B1 | P1 主题基座 | Task 1、2、3 | 简单批 | 3 | 待填 |
| B2 | P1 主题基座 | Task 4、5 | 复杂批 | 2 | 待填 |
| B3 | P1 主题基座 | Task 6、7、8 | 复杂批 | 3 | 待填 |
| B4 | P2 管理增强 | Task 9、10、11 | 复杂批 | 3 | 待填 |
| B5 | P3 明暗与字体 | Task 12、13、14 | 复杂批 | 3 | 待填 |
| B6 | P3 明暗与字体 | Task 15、16、17 | 简单批 | 3 | 待填 |
| B7 | P3 明暗与字体 | Task 18 | 复杂批 | 1 | 待填 |

---

### Task 1: Rust 命名规则校验与菜单标签转换纯函数

**复杂度：简单**（判定依据：单文件纯逻辑，输入完整，无 IPC/集成）。

**Files:**
- Create: `src-tauri/src/io/themes.rs`
- Modify: `src-tauri/src/io/mod.rs`（追加 `pub mod themes;`）

**Interfaces:**
- Consumes: 无（零依赖纯函数）
- Produces（后续任务依赖的精确签名）:

```rust
/// 主题文件名合法性（含 .css 扩展名整串校验）：小写字母+连字符词干、数字/大写/下划线/空段拒绝
pub fn is_valid_theme_file_name(file_name: &str) -> bool
/// 菜单标签转换：连字符分词 + 每词首字母大写（my-first-theme → "My First Theme"）
pub fn theme_menu_label(name: &str) -> String
```

- [ ] **Step 1: 写失败测试**

`src-tauri/src/io/themes.rs` 新建，先只写模块头、两个空实现占位与测试模块（空实现让编译通过、断言失败即 RED——Rust 无「函数不存在」的运行态红，编译错同样计 RED）：

```rust
// 主题目录服务（08 主题）
//
// 职责：命名规则校验与菜单标签转换（T4 纯函数）、目录扫描（T1）、
// 内置主题预置（T1，clean-room 原创 CSS）、主题命令薄壳。
// 线程安全：纯函数无共享状态；监视句柄生命周期见 watch_themes（Task 9）。
// 命名规则保证的附带性质：合法文件名 URL 安全（小写字母+连字符），拼接
// asset 协议 URL 无需编码（D-10）。

/// 主题文件名合法性（AC-T4-2/3，官方规则对齐：非字母字符仅连字符、数字禁用）
///
/// 规则：词干仅小写字母、以连字符分段且分段非空（禁首尾/连续连字符、下划线、
/// 数字、大写），扩展名恒为小写 .css。手写字符循环而非正则：零初始化错误分支
/// （库代码禁 expect），语义与正则 `^[a-z]+(-[a-z]+)*\.css$` 等价。
pub fn is_valid_theme_file_name(file_name: &str) -> bool {
    // 占位：Step 3 实现
    let _ = file_name;
    false
}

/// 菜单标签转换（AC-T4-1）：连字符分词 + 每词首字母大写
///
/// 输入为词干（不含扩展名）；空段已由命名规则排除，此处 filter 为防御。
pub fn theme_menu_label(name: &str) -> String {
    // 占位：Step 3 实现
    let _ = name;
    String::new()
}

#[cfg(test)]
mod tests {
    use super::*;

    // ---- is_valid_theme_file_name ----
    #[test]
    fn valid_names_accepted() {
        assert!(is_valid_theme_file_name("github.css"));
        assert!(is_valid_theme_file_name("my-first-theme.css"));
        assert!(is_valid_theme_file_name("a.css"));
    }

    #[test]
    fn digit_in_name_rejected_ac_t4_2() {
        // 官方规则：数字属非字母字符，含数字的文件名不安装（用户实测回填）
        assert!(!is_valid_theme_file_name("theme2.css"));
        assert!(!is_valid_theme_file_name("my-theme2.css"));
    }

    #[test]
    fn uppercase_rejected_ac_t4_3() {
        assert!(!is_valid_theme_file_name("MyTheme.css"));
        assert!(!is_valid_theme_file_name("my-theme.CSS")); // 扩展名大写同拒
    }

    #[test]
    fn malformed_names_rejected() {
        assert!(!is_valid_theme_file_name("readme.txt")); // 非 css
        assert!(!is_valid_theme_file_name("theme")); // 无扩展名
        assert!(!is_valid_theme_file_name("")); // 空串
        assert!(!is_valid_theme_file_name(".css")); // 空词干
        assert!(!is_valid_theme_file_name("-theme.css")); // 首连字符（空段）
        assert!(!is_valid_theme_file_name("theme-.css")); // 尾连字符
        assert!(!is_valid_theme_file_name("my--theme.css")); // 连续连字符
        assert!(!is_valid_theme_file_name("my_theme.css")); // 下划线
        assert!(!is_valid_theme_file_name("theme.css.bak")); // 扩展名不在末尾
    }

    // ---- theme_menu_label ----
    #[test]
    fn label_splits_and_capitalizes_ac_t4_1() {
        assert_eq!(theme_menu_label("my-first-theme"), "My First Theme");
        assert_eq!(theme_menu_label("github"), "Github");
        assert_eq!(theme_menu_label("a-b-c"), "A B C");
    }
}
```

`src-tauri/src/io/mod.rs` 模块清单追加一行：

```rust
pub mod themes;
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes`
Expected: FAIL（valid_names_accepted / label_splits_and_capitalizes_ac_t4_1 等断言失败——占位实现恒 false/空串）

- [ ] **Step 3: 最小实现**

替换两个占位函数体：

```rust
pub fn is_valid_theme_file_name(file_name: &str) -> bool {
    // 词干 = 剥离末尾 .css；无该后缀直接拒绝（扩展名大小写不放宽——AC-T4-3 同源规则）
    let Some(stem) = file_name.strip_suffix(".css") else {
        return false;
    };
    // 每个连字符分段须非空且全为小写 ASCII 字母：
    // 空段即首尾/连续连字符，非小写字母即数字/大写/下划线等非法字符
    stem.split('-')
        .all(|w| !w.is_empty() && w.chars().all(|c| c.is_ascii_lowercase()))
}

pub fn theme_menu_label(name: &str) -> String {
    name.split('-')
        .filter(|w| !w.is_empty())
        .map(|w| {
            // 首字母大写 + 其余原样（合法词干已保证全小写）
            let mut chars = w.chars();
            match chars.next() {
                Some(first) => first.to_uppercase().collect::<String>() + chars.as_str(),
                None => String::new(),
            }
        })
        .collect::<Vec<_>>()
        .join(" ")
}
```

- [ ] **Step 4: 运行转绿 + clippy + fmt**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
Expected: 全 PASS、无告警

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/io/themes.rs src-tauri/src/io/mod.rs
git commit -m "feat(theme): 命名规则校验与菜单标签转换纯函数——数字/大写拒绝+分词首字母大写"
```

---

### Task 2: Rust scan_themes 目录扫描核心与 DTO

**复杂度：简单**（判定依据：单文件纯函数，显式收 `dir` 参数（D-6），无 IPC）。

**Files:**
- Modify: `src-tauri/src/io/themes.rs`（追加 DTO 与扫描）

**Interfaces:**
- Consumes: Task 1 的 `is_valid_theme_file_name` / `theme_menu_label`
- Produces:

```rust
/// 主题条目 DTO（camelCase 供前端消费；name=文件名词干，为 settings 持久化键）
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ThemeDto {
    pub name: String,
    pub file_name: String,
    pub label: String,
    pub has_user_css: bool,
}

/// 目录扫描结果 DTO
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ThemeListDto {
    /// 主题目录绝对路径（前端 convertFileSrc 的基准）
    pub dir: String,
    /// 合法主题列表（按名称升序）
    pub themes: Vec<ThemeDto>,
    /// base.user.css 是否存在（4 层第 3 层开关）
    pub has_base_user_css: bool,
}

/// 目录扫描（目录缺失则创建；大小写敏感精确匹配 user.css，见 D-8）
pub fn scan_themes(dir: &Path) -> Result<ThemeListDto, String>
```

- [ ] **Step 1: 写失败测试**

`themes.rs` 测试模块追加（tempdir 助手沿用 watch.rs 先例形态）：

```rust
    // ---- scan_themes（测试模块顶部先加 tempdir 助手）----
    fn temp_dir() -> std::path::PathBuf {
        static COUNTER: std::sync::atomic::AtomicU32 = std::sync::atomic::AtomicU32::new(0);
        let n = COUNTER.fetch_add(1, std::sync::atomic::Ordering::SeqCst);
        let dir = std::env::temp_dir().join(format!("markwell-themes-{}-{n}", std::process::id()));
        fs::create_dir_all(&dir).unwrap();
        dir
    }

    #[test]
    fn scan_filters_and_sorts_valid_css_ac_t1_1() {
        let dir = temp_dir();
        fs::write(dir.join("zeta.css"), "x").unwrap();
        fs::write(dir.join("alpha.css"), "x").unwrap();
        fs::write(dir.join("my-first-theme.css"), "x").unwrap();
        // 非法项：数字/大写/非 css/子目录，均不得出现在列表（AC-T4-2/3 的扫描侧证据）
        fs::write(dir.join("theme2.css"), "x").unwrap();
        fs::write(dir.join("NoUpper.css"), "x").unwrap();
        fs::write(dir.join("readme.txt"), "x").unwrap();
        fs::create_dir(dir.join("dir.css")).unwrap();
        let dto = scan_themes(&dir).unwrap();
        let names: Vec<&str> = dto.themes.iter().map(|t| t.name.as_str()).collect();
        assert_eq!(names, vec!["alpha", "my-first-theme", "zeta"]); // 升序 + 过滤
        assert_eq!(dto.themes[1].label, "My First Theme"); // 标签一并输出
        assert_eq!(dto.themes[1].file_name, "my-first-theme.css");
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn scan_detects_user_css_case_sensitive_ac_t6_1_2_3() {
        let dir = temp_dir();
        fs::write(dir.join("solar-mint.css"), "x").unwrap();
        fs::write(dir.join("solar-mint.user.css"), "x").unwrap();
        fs::write(dir.join("base.user.css"), "x").unwrap();
        // 大小写不匹配（Windows 文件系统不敏感，扫描必须精确字符串比对——AC-T6-3）
        fs::write(dir.join("SOLAR-MINT.USER.CSS"), "x").unwrap();
        let dto = scan_themes(&dir).unwrap();
        assert!(dto.has_base_user_css);
        let mint = dto.themes.iter().find(|t| t.name == "solar-mint").unwrap();
        assert!(mint.has_user_css); // 精确命中
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn scan_creates_missing_dir_and_returns_empty() {
        let dir = temp_dir().join("not-yet");
        let dto = scan_themes(&dir).unwrap();
        assert!(dir.is_dir()); // 缺失即建（open_theme_folder 首启边缘依赖）
        assert!(dto.themes.is_empty());
        assert!(!dto.has_base_user_css);
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn dto_serializes_camel_case_wire_shape() {
        // wire 契约钉桩（D-6：命令层不 mock 直呼，序列化形态在此锁死）
        let dir = temp_dir();
        fs::write(dir.join("solar-mint.css"), "x").unwrap();
        fs::write(dir.join("solar-mint.user.css"), "x").unwrap();
        let json = serde_json::to_value(scan_themes(&dir).unwrap()).unwrap();
        assert_eq!(json["hasBaseUserCss"], serde_json::json!(false));
        assert_eq!(json["themes"][0]["fileName"], serde_json::json!("solar-mint.css"));
        assert_eq!(json["themes"][0]["hasUserCss"], serde_json::json!(true));
        assert!(json["dir"].is_string());
        let _ = fs::remove_dir_all(&dir);
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes`
Expected: FAIL（scan_themes 未定义，编译错误即红）

- [ ] **Step 3: 最小实现**

`themes.rs` 头部补 `use std::fs;`、`use std::path::Path;`（`PathBuf` 到 Task 4 `themes_dir` 落地时再引入，避免 clippy unused-import 告警）、`use serde::Serialize;`，主体追加：

```rust
/// 主题条目 DTO（camelCase 供前端消费；name=文件名词干，为 settings 持久化键）
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ThemeDto {
    /// 主题名（文件名词干，settings.theme 存储键）
    pub name: String,
    /// 文件名（含 .css；asset link 拼接用）
    pub file_name: String,
    /// 菜单标签（连字符分词首字母大写，Themes 菜单直接展示）
    pub label: String,
    /// 同名词干 .user.css 是否存在（4 层第 4 层开关）
    pub has_user_css: bool,
}

/// 目录扫描结果 DTO
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ThemeListDto {
    /// 主题目录绝对路径（前端 convertFileSrc 基准；asset 授权与 link 拼接共用）
    pub dir: String,
    /// 合法主题列表（按名称升序，菜单展示序）
    pub themes: Vec<ThemeDto>,
    /// base.user.css 是否存在（4 层第 3 层开关）
    pub has_base_user_css: bool,
}

/// 目录扫描（T1：合法 .css 过滤 + 排序 + user.css 探测）
///
/// 目录缺失则创建（首启预置/打开目录前的边缘兜底）。
/// 大小写敏感：Windows 文件系统大小写不敏感，`Path::exists()` 会误命中
/// 大小写不同的 {theme}.user.css（AC-T6-3）——一次 read_dir 收集文件名后
/// 精确字符串比对（D-8）。
pub fn scan_themes(dir: &Path) -> Result<ThemeListDto, String> {
    fs::create_dir_all(dir).map_err(|e| format!("创建主题目录失败: {e}"))?;
    let entries = fs::read_dir(dir).map_err(|e| format!("读取主题目录失败: {e}"))?;
    let mut names: Vec<String> = Vec::new();
    for entry in entries {
        let Ok(entry) = entry else { continue };
        // 仅普通文件（用户误放子目录/符号链接不参与匹配）
        if !entry.file_type().map(|t| t.is_file()).unwrap_or(false) {
            continue;
        }
        names.push(entry.file_name().to_string_lossy().into_owned());
    }
    let mut themes: Vec<ThemeDto> = Vec::new();
    for file_name in &names {
        if !is_valid_theme_file_name(file_name) {
            continue;
        }
        // 命名规则已保证 .css 后缀存在，match 兜底防御性跳过（禁 unwrap）
        let Some(name) = file_name.strip_suffix(".css") else { continue };
        let has_user_css = names.iter().any(|n| n == &format!("{name}.user.css"));
        themes.push(ThemeDto {
            name: name.to_string(),
            file_name: file_name.clone(),
            label: theme_menu_label(name),
            has_user_css,
        });
    }
    themes.sort_by(|a, b| a.name.cmp(&b.name));
    let has_base_user_css = names.iter().any(|n| n == "base.user.css");
    Ok(ThemeListDto {
        dir: dir.to_string_lossy().into_owned(),
        themes,
        has_base_user_css,
    })
}
```

- [ ] **Step 4: 运行转绿 + clippy + fmt**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: 全 PASS

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/io/themes.rs
git commit -m "feat(theme): 主题目录扫描核心——合法过滤/升序/user.css 大小写敏感精确匹配/camelCase DTO"
```

---

### Task 3: 内置原创主题 CSS 与启动预置函数

**复杂度：简单**（判定依据：2 个资产文件 + 1 个纯函数，输入完整；clean-room 原创内容随计划给出）。

**Files:**
- Create: `src-tauri/assets/themes/markwell-light.css`
- Create: `src-tauri/assets/themes/markwell-dark.css`
- Modify: `src-tauri/src/io/themes.rs`（追加 `BUILT_IN_THEMES` + `ensure_builtin_themes`）

**Interfaces:**
- Consumes: Crepe 22 变量体系（依赖面核查 13）；D-4 双选择器作用域约定
- Produces:

```rust
/// 内置主题表（词干, CSS 全文；include_str! 编译期嵌入，打包零配置）
pub const BUILT_IN_THEMES: &[(&str, &str)]
/// 预置内置主题到 dir（仅缺失才写——用户修改不覆盖）；目录缺失则建
pub fn ensure_builtin_themes(dir: &Path) -> Result<(), String>
```

- [ ] **Step 1: 写失败测试**

`themes.rs` 测试模块追加：

```rust
    // ---- ensure_builtin_themes ----
    #[test]
    fn builtin_seeded_when_missing() {
        let dir = temp_dir();
        ensure_builtin_themes(&dir).unwrap();
        for (name, css) in BUILT_IN_THEMES {
            let path = dir.join(format!("{name}.css"));
            assert!(path.is_file(), "内置主题 {name} 未预置");
            assert_eq!(fs::read_to_string(&path).unwrap(), *css);
        }
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn builtin_existing_file_not_overwritten() {
        // 用户可能修改内置主题（Typora 惯例）：已存在不覆盖
        let dir = temp_dir();
        fs::write(dir.join("markwell-light.css"), "/* 用户自定义 */").unwrap();
        ensure_builtin_themes(&dir).unwrap();
        assert_eq!(
            fs::read_to_string(dir.join("markwell-light.css")).unwrap(),
            "/* 用户自定义 */"
        );
        // 另一套（缺失）仍正常预置
        assert!(dir.join("markwell-dark.css").is_file());
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn builtin_ensure_creates_missing_dir() {
        let dir = temp_dir().join("nested/themes");
        ensure_builtin_themes(&dir).unwrap();
        assert!(dir.join("markwell-light.css").is_file());
        let _ = fs::remove_dir_all(&dir);
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes`
Expected: FAIL（`BUILT_IN_THEMES`/`ensure_builtin_themes` 未定义）

- [ ] **Step 3: 落地两套原创主题 CSS 与实现**

`src-tauri/assets/themes/markwell-light.css`（clean-room 原创，未复制任何 Typora 主题文件）：

```css
/* MarkWell 内置亮色主题 markwell-light（08 原创编写，clean-room：
   以「清爽纸面 + 蓝色强调」风格原创设计，未复制任何 Typora 主题文件）。
   作用域约定（D-4）：.milkdown 与 .markwell-dark .milkdown 双选择器——
   前者与 crepe-overrides.css 亮色默认段同特异性、link 注入更晚取胜；
   后者与 crepe-overrides 暗色段同特异性打平后凭注入顺序取胜（AC-T5）。
   字号统一 rem（AC-T8-2）：字号偏好调整根字号时全文字号跟随。 */
.milkdown,
.markwell-dark .milkdown {
  --crepe-color-background: #ffffff;
  --crepe-color-on-background: #1f2328;
  --crepe-color-surface: #f6f8fa;
  --crepe-color-surface-low: #eef1f5;
  --crepe-color-on-surface: #1f2328;
  --crepe-color-on-surface-variant: #59636e;
  --crepe-color-outline: #d1d9e0;
  --crepe-color-primary: #0969da;
  --crepe-color-secondary: #ddf4ff;
  --crepe-color-on-secondary: #0a3069;
  --crepe-color-inverse: #1f2328;
  --crepe-color-on-inverse: #ffffff;
  --crepe-color-inline-code: #8a5300;
  --crepe-color-error: #cf222e;
  --crepe-color-hover: #f6f8fa;
  --crepe-color-selected: #ddf4ff;
  --crepe-color-inline-area: #e7ebef;

  --crepe-font-title: var(--markwell-font-sans, sans-serif);
  --crepe-font-default: var(--markwell-font-sans, sans-serif);
  --crepe-font-code: var(--markwell-font-code, monospace);

  --crepe-shadow-1: 0 1px 2px rgb(31 35 40 / 0.08);
  --crepe-shadow-2: 0 8px 24px rgb(31 35 40 / 0.16);

  /* 基准字号 1rem：字号偏好（10 设置模块）调整根字号时全文跟随（AC-T8-2） */
  --crepe-base-font-size: 1rem;
}
```

`src-tauri/assets/themes/markwell-dark.css`：

```css
/* MarkWell 内置暗色主题 markwell-dark（08 原创编写，clean-room：
   以「深夜墨面 + 青色强调」风格原创设计，未复制任何 Typora 主题文件。
   作用域与 rem 字号约定同 markwell-light.css 文件头注释）。 */
.milkdown,
.markwell-dark .milkdown {
  --crepe-color-background: #101418;
  --crepe-color-on-background: #e6e8eb;
  --crepe-color-surface: #171c22;
  --crepe-color-surface-low: #1e242b;
  --crepe-color-on-surface: #e6e8eb;
  --crepe-color-on-surface-variant: #9ea7b1;
  --crepe-color-outline: #333b44;
  --crepe-color-primary: #2dd4bf;
  --crepe-color-secondary: #0f3d38;
  --crepe-color-on-secondary: #ccfbf1;
  --crepe-color-inverse: #ccfbf1;
  --crepe-color-on-inverse: #0f3d38;
  --crepe-color-inline-code: #f59e0b;
  --crepe-color-error: #f87171;
  --crepe-color-hover: #1c232a;
  --crepe-color-selected: #14453f;
  --crepe-color-inline-area: #232b33;

  --crepe-font-title: var(--markwell-font-sans, sans-serif);
  --crepe-font-default: var(--markwell-font-sans, sans-serif);
  --crepe-font-code: var(--markwell-font-code, monospace);

  --crepe-shadow-1: 0 1px 2px rgb(0 0 0 / 0.4);
  --crepe-shadow-2: 0 8px 24px rgb(0 0 0 / 0.5);

  --crepe-base-font-size: 1rem;
}
```

`themes.rs` 主体追加（use 补 `use std::path::Path;` 若 Task 2 未含）：

```rust
/// 内置主题表（词干, CSS 全文）：include_str! 编译期嵌入二进制，打包零配置（D-1）
pub const BUILT_IN_THEMES: &[(&str, &str)] = &[
    (
        "markwell-light",
        include_str!("../../assets/themes/markwell-light.css"),
    ),
    (
        "markwell-dark",
        include_str!("../../assets/themes/markwell-dark.css"),
    ),
];

/// 预置内置主题到主题目录（T1：亮/暗各一套起步）
///
/// 仅缺失才写——用户对内置主题的修改不被启动覆盖；目录缺失则建。
/// @returns 失败返回中文错误（setup 钩子调用方记录日志并降级，不阻断启动）
pub fn ensure_builtin_themes(dir: &Path) -> Result<(), String> {
    fs::create_dir_all(dir).map_err(|e| format!("创建主题目录失败: {e}"))?;
    for (name, css) in BUILT_IN_THEMES {
        let target = dir.join(format!("{name}.css"));
        if target.exists() {
            continue;
        }
        fs::write(&target, css).map_err(|e| format!("预置内置主题失败({name}): {e}"))?;
    }
    Ok(())
}
```

- [ ] **Step 4: 运行转绿 + clippy + fmt**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
Expected: 全 PASS

- [ ] **Step 5: 提交**

```bash
git add src-tauri/assets/themes/markwell-light.css src-tauri/assets/themes/markwell-dark.css src-tauri/src/io/themes.rs
git commit -m "feat(theme): 内置原创亮/暗主题（Crepe 22 变量 rem 字号）+ 启动预置函数"
```

---

### Task 4: list_themes / open_theme_folder 命令、注册、setup 预置与 CSP 放行

**复杂度：复杂**（判定依据：跨 IPC 注册、多文件集成——lib.rs Builder 链/setup 钩子、capability、tauri.conf.json）。

**Files:**
- Modify: `src-tauri/src/io/themes.rs`（`themes_dir` + 两条命令）
- Modify: `src-tauri/src/lib.rs`（setup 钩子 + generate_handler 追加 2 条）
- Modify: `src-tauri/capabilities/default.json`（追加 2 权限）
- Modify: `src-tauri/tauri.conf.json`（CSP style-src/font-src 放行 asset:）

**Interfaces:**
- Consumes: Task 2 `scan_themes`、Task 3 `ensure_builtin_themes`；tauri-plugin-opener `OpenerExt`（D-2）
- Produces（wire 契约，Task 6 前端按此建模）:

```rust
/// 主题目录定位：app_data_dir/themes（不创建——创建职责在 scan/open 内）
pub fn themes_dir<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> Result<PathBuf, String>
/// 命令：扫描主题列表（目录缺失自动创建）
#[tauri::command]
pub fn list_themes<R: tauri::Runtime>(app: tauri::AppHandle<R>) -> Result<ThemeListDto, String>
/// 命令：资源管理器打开主题目录（目录缺失自动创建；open_path 打开目录本身）
#[tauri::command]
pub fn open_theme_folder<R: tauri::Runtime>(app: tauri::AppHandle<R>) -> Result<(), String>
```

- [ ] **Step 1: 写失败测试（可测面的序列化与纯函数已锁，命令薄壳按 D-6 豁免 mock 直呼）**

`themes.rs` 测试模块追加唯一可直呼面（`themes_dir` 与命令含 AppHandle 路径解析，按 D-6 不 mock 直呼——注释说明理由）：

```rust
    // 命令层（list_themes/open_theme_folder）含 app.path().app_data_dir() 路径解析，
    // mock 运行时 identifier 为空串会解析到真实 %APPDATA%（D-6 已核验），禁直呼——
    // wire 契约由 dto_serializes_camel_case_wire_shape（Task 2）+ E2E（Task 18）实证。
    // 此处仅钉 themes_dir 的相对拼接规则（借 mock 运行时验证 join 逻辑、不触盘）：
    #[test]
    fn themes_dir_joins_themes_under_app_data() {
        let app = tauri::test::mock_app();
        let dir = themes_dir(app.handle()).unwrap();
        // identifier 为空 → 目录为 data_dir 本身 + "themes"；仅断言尾部段拼接关系
        assert!(dir.ends_with("themes"));
        assert!(dir.to_string_lossy().ends_with("themes"));
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes`
Expected: FAIL（`themes_dir` 未定义）

- [ ] **Step 3: 实现**

`themes.rs` 主体追加（头部 use 扩展：Task 2 的 `use std::path::Path;` 扩为 `use std::path::{Path, PathBuf};`——`themes_dir` 返回值引入 PathBuf；无需 `assert_safe_path`：主题目录路径来自 app 数据目录解析而非用户输入）：

```rust
/// 主题目录名（app 数据目录下相对路径）
pub const THEMES_DIR_NAME: &str = "themes";

/// 主题目录定位：app_data_dir/themes（不触盘创建；创建职责在 scan/open 内）
pub fn themes_dir<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> Result<PathBuf, String> {
    use tauri::Manager;
    let base = app
        .path()
        .app_data_dir()
        .map_err(|e| format!("解析应用数据目录失败: {e}"))?;
    Ok(base.join(THEMES_DIR_NAME))
}

/// 命令：扫描主题列表（Themes 菜单数据源；目录缺失自动创建）
#[tauri::command]
pub fn list_themes<R: tauri::Runtime>(app: tauri::AppHandle<R>) -> Result<ThemeListDto, String> {
    scan_themes(&themes_dir(&app)?)
}

/// 命令：资源管理器打开主题目录（AC-T2-1）
///
/// UI 触发入口（Appearance 分区按钮）归 10/12——本命令为预留接口（见 TODO）。
/// 目录缺失先建（首启未扫描即点开的边缘）；open_path 打开目录本身对齐
/// Typora「打开主题目录」语义（D-2）。
#[tauri::command]
pub fn open_theme_folder<R: tauri::Runtime>(app: tauri::AppHandle<R>) -> Result<(), String> {
    use tauri_plugin_opener::OpenerExt;
    // TODO(theme-ui): open_theme_folder 由 10 设置页 Appearance 分区 / 12 菜单装配消费，计划于 10/12 模块接入
    let dir = themes_dir(&app)?;
    fs::create_dir_all(&dir).map_err(|e| format!("创建主题目录失败: {e}"))?;
    app.opener()
        .open_path(dir.to_string_lossy(), None::<&str>)
        .map_err(|e| format!("打开主题目录失败: {e}"))
}
```

`src-tauri/src/lib.rs` 两处修改——①`manage(AppState {...})` 之后追加 setup 钩子：

```rust
        .setup(|app| {
            // 08 主题：预置内置主题到数据目录 themes/（缺失才写，D-1）。
            // 预置失败仅记录不阻断启动（主题降级为空列表，编辑主链路不受影响；
            // 宪法 A.5.7：setup 返回 Err 表启动失败，此处属可降级路径不失败）
            let seeded =
                io::themes::themes_dir(app.handle()).and_then(|dir| io::themes::ensure_builtin_themes(&dir));
            if let Err(e) = seeded {
                eprintln!("[MarkWell] 内置主题预置失败（主题功能降级）: {e}");
            }
            Ok(())
        })
```

②`generate_handler!` 数组**末尾追加**（A.3.1 只允许追加）：

```rust
            io::themes::list_themes,
            io::themes::open_theme_folder
```

（注意把原末项 `io::images::allow_asset_directory` 后补逗号。）

`src-tauri/capabilities/default.json` permissions 数组**末尾追加**：

```json
    "allow-list-themes",
    "allow-open-theme-folder"
```

`src-tauri/tauri.conf.json` 的 `csp` 改为（仅增 style-src/font-src 两个来源，其余逐字保留）：

```json
      "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' asset: http://asset.localhost; img-src 'self' data: blob: asset: http://asset.localhost; font-src 'self' data: asset: http://asset.localhost; connect-src ipc: http://ipc.localhost ws://localhost:1421 http://localhost:1421; object-src 'none'; base-uri 'none'; frame-ancestors 'none'",
```

- [ ] **Step 4: 运行转绿 + 全量 Rust 门禁**

Run: `cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
Expected: 全 PASS（注册新命令后全量回归，防 generate_handler 宏遗漏）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/io/themes.rs src-tauri/src/lib.rs src-tauri/capabilities/default.json src-tauri/tauri.conf.json
git commit -m "feat(theme): list_themes/open_theme_folder 命令接线——setup 预置内置主题+CSP 放行 asset 样式字体"
```

---

### Task 5: settings 服务 theme 组扩展

**复杂度：简单**（判定依据：2 文件、模式完全照 image 组先例，输入完整）。

**Files:**
- Modify: `src/services/settings.ts`
- Modify: `src/services/settings.spec.ts`（追加用例）

**Interfaces:**
- Consumes: 既有 loadSettings/updateSettings 收口机制（宪法 A.2.7 store 持久化）
- Produces（Task 8 themeStore 消费）:

```typescript
/** 主题设置（08；明暗分离存储） */
export interface ThemeSettings {
  /** 亮色模式主题名（文件名词干） */
  lightTheme: string;
  /** 暗色模式主题名 */
  darkTheme: string;
}
// AppSettings 增加 theme: ThemeSettings；
// DEFAULT_SETTINGS.theme = { lightTheme: "markwell-light", darkTheme: "markwell-dark" }（与 Rust BUILT_IN_THEMES 同名）；
// updateSettings patch Omit 联合增加 "theme"，独立声明 theme?: Partial<ThemeSettings>
```

- [ ] **Step 1: 写失败测试**

`settings.spec.ts` 的 describe 内追加两用例（沿 image 组用例形态）：

```typescript
  it("theme 组缺失键逐层回落默认（亮暗各内置主题名）", async () => {
    // 无存储键（beforeEach 已清空）→ theme 组逐字段回落默认值：
    // 默认名与 Rust BUILT_IN_THEMES 预置文件同名，首启即可解析到主题
    const s = await loadSettings();
    expect(s.theme).toEqual(DEFAULT_SETTINGS.theme);
    expect(s.theme.lightTheme).toBe("markwell-light");
    expect(s.theme.darkTheme).toBe("markwell-dark");
  });

  it("updateSettings 增量合并 theme 字段且不影响其他组", async () => {
    const s = await updateSettings({ theme: { lightTheme: "solar-mint" } });
    expect(s.theme.lightTheme).toBe("solar-mint");
    expect(s.theme.darkTheme).toBe("markwell-dark"); // 未触及键保持
    expect(s.autoSave.enabled).toBe(true); // 其他组不受影响
    const reloaded = await loadSettings();
    expect(reloaded.theme).toEqual({ lightTheme: "solar-mint", darkTheme: "markwell-dark" }); // 整组写回独立键 theme
  });
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/services/settings.spec.ts`
Expected: FAIL（theme 字段不存在）

- [ ] **Step 3: 实现**

`settings.ts` 六处增量（照 image 组先例）：

①文件头注释键值清单加一行：`// theme（主题：亮/暗模式主题名）、`；
②`ImageSettings` 接口后追加：

```typescript
/** 主题设置（08；明暗分离存储，mode → 主题名映射） */
export interface ThemeSettings {
  /** 亮色模式主题名（文件名词干；设置值失效时由主题层回落内置主题） */
  lightTheme: string;
  /** 暗色模式主题名 */
  darkTheme: string;
}
```

③`AppSettings` 增加 `theme: ThemeSettings;`（含注释 `/** 主题（08 模块消费） */`）；
④`DEFAULT_SETTINGS` 增加：

```typescript
  theme: { lightTheme: "markwell-light", darkTheme: "markwell-dark" },
```

⑤`loadSettings` 的 stored 对象增加 `theme: ((await store.get("theme")) ?? {}) as Partial<ThemeSettings>,`，返回值增加：

```typescript
    theme: {
      lightTheme: stored.theme.lightTheme ?? DEFAULT_SETTINGS.theme.lightTheme,
      darkTheme: stored.theme.darkTheme ?? DEFAULT_SETTINGS.theme.darkTheme,
    },
```

⑥`updateSettings`：patch 类型改为 `Partial<Omit<AppSettings, "launch" | "outline" | "image" | "theme">> & { launch?: ...; outline?: ...; image?: ...; theme?: Partial<ThemeSettings>; }`；`next` 增加 `theme: { ...current.theme, ...patch.theme },`；写回增加 `await store.set("theme", next.theme);`。

- [ ] **Step 4: 运行转绿 + 类型检查**

Run: `npx vitest run src/services/settings.spec.ts && npm run typecheck`
Expected: PASS（typecheck 覆盖 patch Omit 联合扩展的编译面）

- [ ] **Step 5: 提交**

```bash
git add src/services/settings.ts src/services/settings.spec.ts
git commit -m "feat(theme): 设置服务扩展 theme 组——亮暗分离主题名（默认内置同名，UI 归 10 模块）"
```

---

### Task 6: 前端主题域 IPC 收敛服务

**复杂度：简单**（判定依据：2 文件纯封装，invoke 契约以 mock 测试锁定，不依赖 Rust 侧编译）。

**Files:**
- Create: `src/services/theme-io.ts`
- Create: `src/services/theme-io.spec.ts`

**Interfaces:**
- Consumes: `@tauri-apps/api/core`（invoke/Channel/convertFileSrc 由调用方使用）；Task 4 wire 契约
- Produces（Task 7/8/14 消费的精确签名）:

```typescript
/** 主题元数据（list_themes 行） */
export interface ThemeMeta {
  /** 主题名（文件名词干，settings 持久化键） */
  name: string;
  /** 文件名（含 .css） */
  fileName: string;
  /** 菜单标签 */
  label: string;
  /** {theme}.user.css 是否存在 */
  hasUserCss: boolean;
}
/** list_themes 响应（无参命令，无请求建模——请求/响应分离） */
export interface ThemeListResult {
  /** 主题目录绝对路径（convertFileSrc 基准） */
  dir: string;
  /** 合法主题列表（升序） */
  themes: ThemeMeta[];
  /** base.user.css 是否存在 */
  hasBaseUserCss: boolean;
}
/** watch_themes 事件（Rust 100ms 合并批量投递） */
export interface ThemeFsEvent {
  /** create / remove / modify / other */
  kind: string;
  /** 变更条目完整路径 */
  path: string;
}
export class ThemeIoError extends Error
export function listThemes(): Promise<ThemeListResult>
export function openThemeFolder(): Promise<void>
export function watchThemes(onEvents: (events: ThemeFsEvent[]) => void): Promise<void>
export function toggleDevtools(): Promise<boolean>   // Task 14 消费（命令 Task 14 注册）
export function allowThemeAssetDirectory(dir: string): Promise<void>  // 复用 07 命令
```

- [ ] **Step 1: 写失败测试**

```typescript
// src/services/theme-io.spec.ts
// 主题域 IPC 收敛（08 spec §5）：命令名/wire 形状/错误规范化三面钉桩。
// mock invoke 层验证——不依赖 Rust 侧编译（命令实现见 Task 4/9/14）。
import { beforeEach, describe, expect, it, vi } from "vitest";

const invokeMock = vi.hoisted(() => vi.fn());
vi.mock("@tauri-apps/api/core", () => ({
  invoke: invokeMock,
  Channel: class {
    onmessage: ((events: unknown) => void) | null = null;
  },
}));

import {
  allowThemeAssetDirectory,
  listThemes,
  openThemeFolder,
  ThemeIoError,
  toggleDevtools,
  watchThemes,
} from "./theme-io";

describe("theme-io IPC 收敛", () => {
  beforeEach(() => {
    invokeMock.mockReset();
  });

  it("listThemes 单参无 args 调用并透传响应", async () => {
    const dto = { dir: "C:\\t", themes: [], hasBaseUserCss: false };
    invokeMock.mockResolvedValue(dto);
    await expect(listThemes()).resolves.toEqual(dto);
    expect(invokeMock).toHaveBeenCalledWith("list_themes");
    expect(invokeMock).toHaveBeenCalledTimes(1);
  });

  it("watchThemes 经 Channel 批量接收且 onmessage 已接线", async () => {
    invokeMock.mockResolvedValue(undefined);
    const onEvents = vi.fn();
    await watchThemes(onEvents);
    const [command, args] = invokeMock.mock.calls[0] as [string, { channel: { onmessage: unknown } }];
    expect(command).toBe("watch_themes");
    expect(args.channel.onmessage).toBe(onEvents);
  });

  it("openThemeFolder / toggleDevtools / allowThemeAssetDirectory 命令与参数形状", async () => {
    invokeMock.mockResolvedValue(undefined);
    await openThemeFolder();
    expect(invokeMock).toHaveBeenLastCalledWith("open_theme_folder");
    await allowThemeAssetDirectory("C:\\t");
    expect(invokeMock).toHaveBeenLastCalledWith("allow_asset_directory", { dir: "C:\\t" });
    invokeMock.mockResolvedValue(true);
    await expect(toggleDevtools()).resolves.toBe(true);
    expect(invokeMock).toHaveBeenLastCalledWith("toggle_devtools");
  });

  it("字符串拒绝值规范化为 ThemeIoError（Rust Result Err 中文直传）", async () => {
    invokeMock.mockRejectedValue("主题目录不可读");
    const err = await listThemes().catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ThemeIoError);
    expect((err as ThemeIoError).message).toBe("主题目录不可读");
  });

  it("Error 对象与非 Error 拒绝值分别取 message 与兜底文案", async () => {
    invokeMock.mockRejectedValue(new Error("boom"));
    await expect(listThemes()).rejects.toThrow(ThemeIoError, { message: "boom" });
    invokeMock.mockRejectedValue({ code: 1 });
    await expect(listThemes()).rejects.toThrow(ThemeIoError, { message: "未知主题服务错误" });
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/services/theme-io.spec.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```typescript
// 主题域 IPC 收敛（08 spec §5 提供面；宪法 B.4.1 invoke 收敛服务层）
//
// 契约对齐 Rust io/themes.rs：DTO camelCase（serde rename_all），错误为 Rust 中文
// String → ThemeIoError。不复用 file-io 的 FileIoError——职责隔离（A.7.3），
// 错误类型按域各自建模。请求/响应分离：list_themes 无参（无请求建模），
// watch_themes 经 Channel 批量接收（与 file-io.watchDir 同形态、独立 interface）。
import { Channel, invoke } from "@tauri-apps/api/core";

/** 主题元数据（list_themes 行） */
export interface ThemeMeta {
  /** 主题名（文件名词干，settings 持久化键） */
  name: string;
  /** 文件名（含 .css） */
  fileName: string;
  /** 菜单标签（连字符分词首字母大写） */
  label: string;
  /** {theme}.user.css 是否存在 */
  hasUserCss: boolean;
}

/** list_themes 响应 */
export interface ThemeListResult {
  /** 主题目录绝对路径（convertFileSrc 基准） */
  dir: string;
  /** 合法主题列表（升序） */
  themes: ThemeMeta[];
  /** base.user.css 是否存在 */
  hasBaseUserCss: boolean;
}

/** watch_themes 事件（Rust 100ms 合并窗口批量投递） */
export interface ThemeFsEvent {
  /** create / remove / modify / other */
  kind: string;
  /** 变更条目完整路径 */
  path: string;
}

/** 主题服务错误（invoke 拒绝的规范化包装，message 为 Rust 侧中文错误） */
export class ThemeIoError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "ThemeIoError";
  }
}

/**
 * invoke 包装：Rust 侧 String 错误 → ThemeIoError（与 file-io 同形态、独立域）
 * @param command 已注册的 Tauri command 名
 * @param args 透传参数（camelCase 键）
 */
async function invokeOrThrow<T>(command: string, args?: Record<string, unknown>): Promise<T> {
  try {
    return await (args === undefined ? invoke<T>(command) : invoke<T>(command, args));
  } catch (error) {
    const message =
      typeof error === "string"
        ? error
        : error instanceof Error
          ? error.message
          : "未知主题服务错误";
    throw new ThemeIoError(message);
  }
}

/** 扫描主题列表（Themes 菜单数据源；目录缺失自动创建） */
export function listThemes(): Promise<ThemeListResult> {
  return invokeOrThrow<ThemeListResult>("list_themes");
}

// TODO(theme-ui): openThemeFolder 由 10 设置页 Appearance 分区 / 12 菜单装配消费，计划于 10/12 模块接入
/** 资源管理器打开主题目录（AC-T2-1） */
export function openThemeFolder(): Promise<void> {
  return invokeOrThrow<void>("open_theme_folder");
}

/**
 * 订阅主题目录变更（热刷新事件流；Rust 侧 100ms 合并批量）
 * @param onEvents 批量事件回调（订阅随应用生命周期存活）
 */
export function watchThemes(onEvents: (events: ThemeFsEvent[]) => void): Promise<void> {
  const channel = new Channel<ThemeFsEvent[]>();
  channel.onmessage = onEvents;
  return invokeOrThrow<void>("watch_themes", { channel });
}

/**
 * 切换 DevTools 开合（AC-T7-1）
 * @returns 切换后状态：true=已打开，false=已关闭（或当前构建不支持）
 */
export function toggleDevtools(): Promise<boolean> {
  return invokeOrThrow<boolean>("toggle_devtools");
}

/** 把主题目录加入 asset 协议可读范围（复用 07 allow_asset_directory，递归含 fonts/） */
export function allowThemeAssetDirectory(dir: string): Promise<void> {
  return invokeOrThrow<void>("allow_asset_directory", { dir });
}
```

- [ ] **Step 4: 运行转绿 + lint**

Run: `npx vitest run src/services/theme-io.spec.ts && npx eslint src/services/theme-io.ts src/services/theme-io.spec.ts --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/services/theme-io.ts src/services/theme-io.spec.ts
git commit -m "feat(theme): 主题域 IPC 收敛服务——listThemes/watchThemes/openThemeFolder 契约封装"
```

---

### Task 7: theme-css 四层 CSS 注入服务

**复杂度：复杂**（判定依据：注入顺序是 spec §4 前端核心（AC-T6-1/2/3 + AC-T1-2），link 生命周期语义多分支）。

**Files:**
- Create: `src/features/theme/theme-css.ts`
- Create: `src/features/theme/theme-css.spec.ts`

**Interfaces:**
- Consumes: Task 6 的 `ThemeMeta`（类型）
- Produces（Task 8/10/13 消费）:

```typescript
export const THEME_LINK_ID = "markwell-theme-link";
export const USER_BASE_LINK_ID = "markwell-theme-user-base-link";
export const USER_THEME_LINK_ID = "markwell-theme-user-link";
/** 注入入参（A.7 参数对象化；theme=null 表示无激活主题） */
export interface ActiveThemeRefs {
  /** 主题目录 asset 基准 URL（convertFileSrc 产物，尾随分隔符归一） */
  themesAssetBase: string;
  /** 当前模式激活主题（null → 移除主题层与主题 user 层） */
  theme: Pick<ThemeMeta, "name" | "fileName" | "hasUserCss"> | null;
  /** base.user.css 存在（Rust 扫描） */
  hasBaseUserCss: boolean;
  /** 缓存穿透时间戳（热刷新时更新） */
  cacheBust: number;
}
/** 按四层顺序 upsert/移除 head 内固定 id link（幂等；默认操作当前 document） */
export function applyThemeCss(refs: ActiveThemeRefs, doc?: Document): void
```

- [ ] **Step 1: 写失败测试**

```typescript
// src/features/theme/theme-css.spec.ts
// 四层 CSS 注入（08 spec T6/T1-2）：顺序、条件挂载、大小写敏感的挂载键、
// 热刷新 ?t= 重挂、移除语义。只操作 head 内 link（B.2.5：不触碰编辑器 DOM）。
import { beforeEach, describe, expect, it } from "vitest";
import type { ThemeMeta } from "../../services/theme-io";
import { applyThemeCss, THEME_LINK_ID, USER_BASE_LINK_ID, USER_THEME_LINK_ID } from "./theme-css";

const light: ThemeMeta = {
  name: "markwell-light",
  fileName: "markwell-light.css",
  label: "Markwell Light",
  hasUserCss: true,
};

function linkIds(): string[] {
  return Array.from(document.head.querySelectorAll("link")).map((l) => l.id);
}

function hrefOf(id: string): string | null {
  return (document.getElementById(id) as HTMLLinkElement | null)?.getAttribute("href") ?? null;
}

describe("applyThemeCss 四层注入", () => {
  beforeEach(() => {
    document.head.innerHTML = "";
  });

  it("全层挂载且顺序为 主题 → base.user → {theme}.user（AC-T6-1/2）", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/C%3A%5Cthemes",
      theme: light,
      hasBaseUserCss: true,
      cacheBust: 1,
    });
    expect(linkIds()).toEqual([THEME_LINK_ID, USER_BASE_LINK_ID, USER_THEME_LINK_ID]);
    expect(hrefOf(THEME_LINK_ID)).toBe("http://asset.localhost/C%3A%5Cthemes/markwell-light.css?t=1");
    expect(hrefOf(USER_BASE_LINK_ID)).toBe("http://asset.localhost/C%3A%5Cthemes/base.user.css?t=1");
    expect(hrefOf(USER_THEME_LINK_ID)).toBe("http://asset.localhost/C%3A%5Cthemes/markwell-light.user.css?t=1");
  });

  it("hasBaseUserCss=false 时第 3 层不挂载；hasUserCss=false 时第 4 层不挂载", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: { ...light, hasUserCss: false },
      hasBaseUserCss: false,
      cacheBust: 1,
    });
    expect(linkIds()).toEqual([THEME_LINK_ID]);
  });

  it("theme=null 移除主题层与主题 user 层、保留 base.user 层（全局追加对所有主题生效）", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: light,
      hasBaseUserCss: true,
      cacheBust: 1,
    });
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: null,
      hasBaseUserCss: true,
      cacheBust: 2,
    });
    expect(linkIds()).toEqual([USER_BASE_LINK_ID]);
  });

  it("热刷新：cacheBust 变化重挂同 id link 更新 href（AC-T3-2 机制）", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: light,
      hasBaseUserCss: true,
      cacheBust: 1,
    });
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: light,
      hasBaseUserCss: true,
      cacheBust: 2,
    });
    expect(linkIds()).toEqual([THEME_LINK_ID, USER_BASE_LINK_ID, USER_THEME_LINK_ID]); // 无重复节点
    expect(hrefOf(THEME_LINK_ID)).toBe("http://asset.localhost/t/markwell-light.css?t=2");
  });

  it("尾随分隔符归一：基准带 / 或 \\ 时拼接无重复分隔符", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/C%3A%5Cthemes%5Csub/",
      theme: light,
      hasBaseUserCss: false,
      cacheBust: 1,
    });
    expect(hrefOf(THEME_LINK_ID)).toBe("http://asset.localhost/C%3A%5Cthemes%5Csub/markwell-light.css?t=1");
  });

  it("重复 apply 同 href 幂等不新增节点", () => {
    const refs = {
      themesAssetBase: "http://asset.localhost/t",
      theme: light,
      hasBaseUserCss: false,
      cacheBust: 1,
    } as const;
    applyThemeCss(refs);
    applyThemeCss(refs);
    expect(linkIds()).toEqual([THEME_LINK_ID]);
  });

  it("link 元素 rel=stylesheet 且挂载于 head", () => {
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/t",
      theme: light,
      hasBaseUserCss: false,
      cacheBust: 1,
    });
    const link = document.getElementById(THEME_LINK_ID) as HTMLLinkElement;
    expect(link.rel).toBe("stylesheet");
    expect(link.ownerDocument).toBe(document);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/theme-css.spec.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```typescript
// 主题 CSS 注入服务（08 spec §5「主题 CSS 注入服务」，供 themeStore 驱动）
//
// 四层顺序（spec T6）：第 1 层 基础 = crepe-overrides.css（main.ts 静态引入，本服务
// 不管辖）；第 1.5 层 映射层 = typora-var-bridge.css（静态引入，Task 11）；
// 第 2 层 主题 = {theme}.css；第 3 层 全局追加 = base.user.css（存在才挂）；
// 第 4 层 主题追加 = {theme}.user.css（存在才挂，随主题层移除）。
// 热刷新（D-7）：cacheBust 变化 → 同 id link 换 href（?t= 穿透 WebView 缓存）。
// 红线：只操作 document.head 的 link，绝不触碰编辑器 DOM（宪法 B.2.5）。
// 命名规则（小写字母+连字符）保证 fileName URL 安全，无需 encode（D-10）。
import type { ThemeMeta } from "../../services/theme-io";

/** 主题层 link id */
export const THEME_LINK_ID = "markwell-theme-link";
/** base.user.css 层 link id */
export const USER_BASE_LINK_ID = "markwell-theme-user-base-link";
/** {theme}.user.css 层 link id */
export const USER_THEME_LINK_ID = "markwell-theme-user-link";

/** 注入入参（A.7 参数对象化） */
export interface ActiveThemeRefs {
  /** 主题目录 asset 基准 URL（convertFileSrc 产物） */
  themesAssetBase: string;
  /** 当前模式激活主题；null = 无激活主题（层移除） */
  theme: Pick<ThemeMeta, "name" | "fileName" | "hasUserCss"> | null;
  /** base.user.css 存在（Rust 扫描大小写敏感结果） */
  hasBaseUserCss: boolean;
  /** 缓存穿透时间戳（热刷新时更新） */
  cacheBust: number;
}

/** upsert 或移除固定 id link（href 相同则不动——幂等，重复 apply 零副作用） */
function upsertLink(doc: Document, id: string, href: string | null): void {
  const existing = doc.getElementById(id) as HTMLLinkElement | null;
  if (href === null) {
    existing?.remove();
    return;
  }
  if (!existing) {
    const link = doc.createElement("link");
    link.id = id;
    link.rel = "stylesheet";
    doc.head.appendChild(link);
  }
  const link = doc.getElementById(id) as HTMLLinkElement;
  if (link.getAttribute("href") !== href) {
    link.setAttribute("href", href);
  }
}

/**
 * 按四层顺序重排主题 CSS 链
 * @param refs 见接口注
 * @param doc 注入目标文档（测试注入 jsdom；缺省当前文档）
 */
export function applyThemeCss(refs: ActiveThemeRefs, doc: Document = document): void {
  // 基准归一：剥掉尾随分隔符（asset URL 的路径段为编码形态，/ 与 \ 均可能尾随）
  const base = refs.themesAssetBase.replace(/[/\\]+$/, "");
  upsertLink(
    doc,
    THEME_LINK_ID,
    refs.theme ? `${base}/${refs.theme.fileName}?t=${refs.cacheBust}` : null,
  );
  upsertLink(
    doc,
    USER_BASE_LINK_ID,
    refs.hasBaseUserCss ? `${base}/base.user.css?t=${refs.cacheBust}` : null,
  );
  upsertLink(
    doc,
    USER_THEME_LINK_ID,
    refs.theme?.hasUserCss ? `${base}/${refs.theme.name}.user.css?t=${refs.cacheBust}` : null,
  );
}
```

- [ ] **Step 4: 运行转绿 + lint**

Run: `npx vitest run src/features/theme/theme-css.spec.ts && npx eslint src/features/theme --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/features/theme/theme-css.ts src/features/theme/theme-css.spec.ts
git commit -m "feat(theme): 四层 CSS 注入服务——固定 id link upsert/?t= 防缓存/移除语义"
```

---

### Task 8: themeStore 状态机基座 + App 装配接线

**复杂度：复杂**（判定依据：Pinia store 状态机（spec §4 明暗切换链路的状态侧）+ App.vue 集成 + 07 授权命令复用，多文件）。

**Files:**
- Create: `src/features/theme/theme-store.ts`
- Create: `src/features/theme/theme-store.spec.ts`
- Create: `src/features/theme/register.ts`
- Modify: `src/App.vue`（onMounted 装配一行）

**Interfaces:**
- Consumes: Task 4 命令（list_themes/allow_asset_directory）、Task 5 `ThemeSettings`、Task 6 服务、Task 7 `applyThemeCss`
- Produces（Task 10/13 与 12 菜单消费）:

```typescript
export type ThemeMode = "light" | "dark";
export const useThemeStore = defineStore("theme", () => {
  // state：themes / lightTheme / darkTheme / systemDark / assetBase / hasBaseUserCss（setup store 全量 return）
  async function init(): Promise<void>            // 设置+扫描并行 → 镜像 → asset 授权 → 首次注入
  async function selectTheme(mode: ThemeMode, name: string): Promise<void>  // 持久化 + 立即重挂
  function resolveActiveTheme(mode: ThemeMode): ThemeMeta | null  // 设置值 → 内置兜底 → null
  // Task 10 追加：refresh / scheduleRefresh / dispose
});
// register.ts：export function registerThemeFeature(): void（Task 13 改返回 cleanup）
```

- [ ] **Step 1: 写失败测试**

```typescript
// src/features/theme/theme-store.spec.ts
// 主题状态机基座（08 spec §5 themeStore）：初始化装配/明暗选择持久化/内置回落。
// theme-io/settings/@tauri-apps/api core 三层 mock；applyThemeCss 走真实 DOM（jsdom head）。
import { beforeEach, describe, expect, it, vi } from "vitest";
import { createPinia, setActivePinia } from "pinia";

const mocks = vi.hoisted(() => ({
  listThemes: vi.fn(),
  watchThemes: vi.fn(),
  allowDir: vi.fn(),
  loadSettings: vi.fn(),
  updateSettings: vi.fn(),
  convert: vi.fn((p: string) => `http://asset.localhost/${encodeURIComponent(p)}`),
  errorSpy: vi.fn(),
}));
vi.mock("../../services/theme-io", () => ({
  listThemes: mocks.listThemes,
  watchThemes: mocks.watchThemes,
  allowThemeAssetDirectory: mocks.allowDir,
  openThemeFolder: vi.fn(),
  toggleDevtools: vi.fn(),
}));
vi.mock("../../services/settings", () => ({
  loadSettings: mocks.loadSettings,
  updateSettings: mocks.updateSettings,
}));
vi.mock("@tauri-apps/api/core", () => ({ convertFileSrc: mocks.convert }));

import { useThemeStore } from "./theme-store";

const LIST = {
  dir: "C:\\Users\\t\\AppData\\Roaming\\com.markwell.app\\themes",
  hasBaseUserCss: true,
  themes: [
    { name: "markwell-dark", fileName: "markwell-dark.css", label: "Markwell Dark", hasUserCss: false },
    { name: "markwell-light", fileName: "markwell-light.css", label: "Markwell Light", hasUserCss: true },
    { name: "solar-mint", fileName: "solar-mint.css", label: "Solar Mint", hasUserCss: false },
  ],
};
const SETTINGS = { theme: { lightTheme: "markwell-light", darkTheme: "markwell-dark" } };

describe("themeStore 基座", () => {
  beforeEach(() => {
    setActivePinia(createPinia());
    document.head.innerHTML = "";
    document.documentElement.className = "";
    vi.clearAllMocks();
    mocks.listThemes.mockResolvedValue(LIST);
    mocks.loadSettings.mockResolvedValue(SETTINGS);
    mocks.updateSettings.mockImplementation(async (patch: { theme?: Partial<{ lightTheme: string; darkTheme: string }> }) => ({
      theme: { ...SETTINGS.theme, ...patch.theme },
    }));
    vi.spyOn(console, "error").mockImplementation(mocks.errorSpy);
    vi.spyOn(Date, "now").mockReturnValue(1_000);
  });

  it("init 装配：列表/设置镜像 + asset 授权 + 亮色主题 link 挂载（AC-T1-1/1-2 状态侧）", async () => {
    const store = useThemeStore();
    await store.init();
    expect(store.themes).toHaveLength(3);
    expect(store.lightTheme).toBe("markwell-light");
    expect(store.hasBaseUserCss).toBe(true);
    expect(store.assetBase).toBe(`http://asset.localhost/${encodeURIComponent(LIST.dir)}`);
    expect(mocks.allowDir).toHaveBeenCalledWith(LIST.dir); // 07 命令复用（递归含 fonts/，AC-T8-1 证据之一）
    const link = document.getElementById("markwell-theme-link") as HTMLLinkElement;
    expect(link.getAttribute("href")).toBe(
      `${store.assetBase}/markwell-light.css?t=1000`,
    );
    // 系统亮色（mock 默认无暗色态）→ 不激活 .markwell-dark 根类
    expect(document.documentElement.classList.contains("markwell-dark")).toBe(false);
  });

  it("init 时 asset 授权失败仅记录错误且主题仍注入（降级链路）", async () => {
    mocks.allowDir.mockRejectedValueOnce(new Error("拒绝"));
    const store = useThemeStore();
    await store.init();
    expect(mocks.errorSpy).toHaveBeenCalled();
    expect(document.getElementById("markwell-theme-link")).not.toBeNull();
  });

  it("selectTheme 持久化并立即重挂（AC-T1-2）", async () => {
    const store = useThemeStore();
    await store.init();
    await store.selectTheme("light", "solar-mint");
    expect(mocks.updateSettings).toHaveBeenCalledWith({ theme: { lightTheme: "solar-mint" } });
    const link = document.getElementById("markwell-theme-link") as HTMLLinkElement;
    expect(link.getAttribute("href")).toContain("/solar-mint.css?t=");
  });

  it("selectTheme dark 写入 darkTheme 键（明暗分离存储，AC-T5-1 状态侧）", async () => {
    const store = useThemeStore();
    await store.init();
    await store.selectTheme("dark", "solar-mint");
    expect(mocks.updateSettings).toHaveBeenCalledWith({ theme: { darkTheme: "solar-mint" } });
    expect(store.darkTheme).toBe("solar-mint");
  });

  it("resolveActiveTheme：设置名失效回落内置同名主题（内置也缺则 null）", async () => {
    const store = useThemeStore();
    await store.init();
    expect(store.resolveActiveTheme("light")?.name).toBe("markwell-light");
    // 设置值指向已删除主题 → 回落内置
    store.lightTheme = "deleted-theme";
    expect(store.resolveActiveTheme("light")?.name).toBe("markwell-light");
    // 内置也缺失 → null（主题层移除，默认样式兜底）
    store.themes = [];
    expect(store.resolveActiveTheme("light")).toBeNull();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/theme-store.spec.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`src/features/theme/theme-store.ts`：

```typescript
// 主题状态机（08 spec §5 提供面：themeStore——主题列表/当前亮暗主题/自定义 CSS 状态）
//
// 职责：设置读取（settings.theme 组）→ 目录扫描（list_themes）→ asset 授权（复用 07
// allow_asset_directory，递归放行主题目录含 fonts/）→ 注入驱动（theme-css）→
// .markwell-dark 根类激活（08 自建运行时激活，消费 crepe-overrides 暗色段）。
// 明暗源 = 系统（Task 12/13 接入 matchMedia；本文件先落恒亮色基座——对齐
// zoom-render.spec 钉桩注释「现阶段恒亮色」）。不调用 setTheme 钉死窗口主题（D-3）。
import { defineStore } from "pinia";
import { ref } from "vue";
import { convertFileSrc } from "@tauri-apps/api/core";
import { allowThemeAssetDirectory, listThemes } from "../../services/theme-io";
import type { ThemeMeta } from "../../services/theme-io";
import { loadSettings, updateSettings } from "../../services/settings";
import { applyThemeCss } from "./theme-css";

/** 明暗模式（settings.theme 两组键的选择器） */
export type ThemeMode = "light" | "dark";

/** 内置主题兜底名（存量主题名被删时按模式回落；与 Rust BUILT_IN_THEMES 同名） */
const BUILTIN_FALLBACK: Record<ThemeMode, string> = {
  light: "markwell-light",
  dark: "markwell-dark",
};

export const useThemeStore = defineStore("theme", () => {
  /** 主题列表（list_themes 扫描结果；Themes 菜单数据源，12 菜单装配消费） */
  const themes = ref<ThemeMeta[]>([]);
  /** 亮色模式主题名（settings.theme.lightTheme 镜像） */
  const lightTheme = ref("");
  /** 暗色模式主题名（settings.theme.darkTheme 镜像） */
  const darkTheme = ref("");
  /** 当前系统暗色（Task 13 起接入 matchMedia 真值；基座恒 false） */
  const systemDark = ref(false);
  /** 主题目录 asset 基准 URL（convertFileSrc(dir)，尾随分隔符归一） */
  const assetBase = ref("");
  /** base.user.css 是否存在（Rust 扫描，4 层第 3 层开关） */
  const hasBaseUserCss = ref(false);

  /** 按名精确查找主题（主题名经 Rust 命名规则保证小写） */
  function byName(name: string): ThemeMeta | undefined {
    return themes.value.find((t) => t.name === name);
  }

  /**
   * 解析当前模式激活主题：设置值优先 → 内置同模式兜底 → null（无主题层，默认样式兜底）
   * @param mode 明暗模式
   */
  function resolveActiveTheme(mode: ThemeMode): ThemeMeta | null {
    const wanted = mode === "light" ? lightTheme.value : darkTheme.value;
    return byName(wanted) ?? byName(BUILTIN_FALLBACK[mode]) ?? null;
  }

  /** 按当前系统色系重挂 CSS 链 + 切换 .markwell-dark 根类（唯一注入出口） */
  function applyCurrent(): void {
    const theme = resolveActiveTheme(systemDark.value ? "dark" : "light");
    applyThemeCss({
      themesAssetBase: assetBase.value,
      theme,
      hasBaseUserCss: hasBaseUserCss.value,
      cacheBust: Date.now(),
    });
    // .markwell-dark 运行时激活（08 自建）：crepe-overrides 暗色段随根类生效；
    // 主题 CSS 双选择器段（D-4）凭注入顺序覆盖其上
    document.documentElement.classList.toggle("markwell-dark", systemDark.value);
  }

  /**
   * 初始化（App 装配调一次）：设置 + 目录扫描并行 → 镜像状态 → asset 授权 → 首次注入。
   * 授权失败仅记录（主题 CSS 将被 asset 协议拒绝，界面回落默认样式），不中断编辑主链路。
   */
  async function init(): Promise<void> {
    const [settings, list] = await Promise.all([loadSettings(), listThemes()]);
    lightTheme.value = settings.theme.lightTheme;
    darkTheme.value = settings.theme.darkTheme;
    themes.value = list.themes;
    hasBaseUserCss.value = list.hasBaseUserCss;
    assetBase.value = convertFileSrc(list.dir).replace(/[/\\]+$/, "");
    try {
      await allowThemeAssetDirectory(list.dir);
    } catch (e) {
      console.error("[MarkWell] 主题目录 asset 授权失败（主题降级为默认样式）", e);
    }
    applyCurrent();
  }

  /**
   * 选择主题（12 菜单/10 设置页消费）：持久化到 settings.theme 组 → 镜像 → 立即重挂
   * @param mode 明暗模式（明暗分离存储）
   * @param name 主题名（文件名词干；须来自 themes 列表）
   */
  async function selectTheme(mode: ThemeMode, name: string): Promise<void> {
    const patch = mode === "light" ? { lightTheme: name } : { darkTheme: name };
    const next = await updateSettings({ theme: patch });
    lightTheme.value = next.theme.lightTheme;
    darkTheme.value = next.theme.darkTheme;
    applyCurrent();
  }

  return {
    themes,
    lightTheme,
    darkTheme,
    systemDark,
    assetBase,
    hasBaseUserCss,
    init,
    selectTheme,
    resolveActiveTheme,
  };
});
```

`src/features/theme/register.ts`：

```typescript
// 主题功能装配（08；App.vue onMounted 调一次。12 窗口外壳后续接管菜单接线）
import { useThemeStore } from "./theme-store";

/**
 * 注册主题功能：初始化主题 store（读设置 + 扫描目录 + asset 授权 + 首次注入）。
 * 异步流程不阻塞启动；失败经 store 内日志降级（编辑主链路不受影响）。
 */
export function registerThemeFeature(): void {
  const store = useThemeStore();
  void store.init();
}
```

`src/App.vue`：script 头部 import 区追加 `import { registerThemeFeature } from "./features/theme/register";`；`onMounted` 内 `registerImageFeature();` 之后追加：

```typescript
  // 08 主题装配：内置主题注入 + 后续热刷新/明暗联动（失败经 store 内日志降级）
  registerThemeFeature();
```

- [ ] **Step 4: 运行转绿 + 类型检查 + lint**

Run: `npx vitest run src/features/theme/theme-store.spec.ts && npm run typecheck && npx eslint src/features/theme src/App.vue --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/features/theme/theme-store.ts src/features/theme/theme-store.spec.ts src/features/theme/register.ts src/App.vue
git commit -m "feat(theme): themeStore 状态机基座——设置镜像/内置回落/asset 授权/App 装配"
```

---

### Task 9: Rust watch_themes 命令与 AppState 主题监视槽位

**复杂度：复杂**（判定依据：跨 IPC（Channel）、监听生命周期（AppState 槽位）、波及 lib.rs 与两处 mock 测试构造）。

**Files:**
- Modify: `src-tauri/src/io/themes.rs`（`start_theme_watch` + `watch_themes` 命令）
- Modify: `src-tauri/src/lib.rs`（AppState 增 `theme_watcher` 字段 + manage 初始化 + 注册命令）
- Modify: `src-tauri/src/io/watch.rs:305`（mock AppState 构造同步）
- Modify: `src-tauri/src/io/search.rs:668`（mock AppState 构造同步）
- Modify: `src-tauri/capabilities/default.json`（`allow-watch-themes`）

**Interfaces:**
- Consumes: `watch::watch_dir_inner`（pub）、`watch::flush_loop`（pub(super)，io 模块树内可见）、`commands::WatchEvent`
- Produces:

```rust
/// 命令：订阅主题目录变更（单槽替换；100ms 合并批量投递）
#[tauri::command]
pub fn watch_themes<R: tauri::Runtime>(
    app: tauri::AppHandle<R>,
    channel: tauri::ipc::Channel<Vec<WatchEvent>>,
) -> Result<(), String>
/// 内部：显式 dir 可测面（D-6；事件经合并窗口批量投递，返回须持活的句柄）
fn start_theme_watch(dir: &Path, channel: tauri::ipc::Channel<Vec<WatchEvent>>) -> Result<RecommendedWatcher, String>
// AppState 新增字段：pub theme_watcher: Mutex<Option<notify::RecommendedWatcher>>
```

- [ ] **Step 1: 写失败测试**

`themes.rs` 测试模块追加（use 补 `use std::sync::mpsc;`、`use std::time::{Duration, Instant};`、`use tauri::ipc::Channel;` 按需）：

```rust
    // ---- start_theme_watch（显式 dir 可测面；命令层按 D-6 豁免 mock 直呼）----
    #[test]
    fn start_theme_watch_delivers_batches() {
        let dir = temp_dir();
        let got: std::sync::Arc<std::sync::Mutex<Vec<Vec<WatchEvent>>>> =
            std::sync::Arc::new(std::sync::Mutex::new(Vec::new()));
        let sink = got.clone();
        let channel = Channel::<Vec<WatchEvent>>::new(move |batch| {
            sink.lock().unwrap().push(batch);
            Ok(())
        });
        let watcher = start_theme_watch(&dir, channel).unwrap();
        fs::write(dir.join("new-theme.css"), "x").unwrap();
        // 事件经 100ms 合并窗口异步到达，轮询等待（沿 watch.rs 先例）
        let deadline = Instant::now() + Duration::from_secs(5);
        loop {
            let batches = got.lock().unwrap().clone();
            if batches.iter().any(|b| b.iter().any(|ev| ev.path.contains("new-theme.css"))) {
                break;
            }
            if Instant::now() >= deadline {
                panic!("等待主题目录变更事件超时");
            }
            std::thread::sleep(Duration::from_millis(50));
        }
        drop(watcher); // 句柄持活语义：drop 即停止（测试收尾）
        let _ = fs::remove_dir_all(&dir);
    }

    #[test]
    fn start_theme_watch_missing_dir_rejected() {
        let channel = Channel::<Vec<WatchEvent>>::new(|_| Ok(()));
        assert!(start_theme_watch(std::path::Path::new("Z:/no-such-dir-xyz"), channel).is_err());
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml io::themes`
Expected: FAIL（`start_theme_watch` 未定义）

- [ ] **Step 3: 实现**

`themes.rs` 头部 use 补（注意：不引入 `std::sync::Mutex`——本文件仅经 guard 调 `lock()`，Mutex 类型名不出现在本文件，多余引入触发 clippy -D warnings）：

```rust
use tauri::ipc::Channel;

use crate::io::commands::WatchEvent;
use crate::io::watch::{flush_loop, watch_dir_inner};
```

测试模块顶部补时间工具导入（事件轮询等待用）：

```rust
    use std::time::{Duration, Instant};
```

主体追加：

```rust
/// 建立主题目录监视（显式 dir 可测面；事件经 mpsc + 合并窗口批量投递）
///
/// 与 io::watch::watch_dir 同一接线形态（mpsc 汇集 + flush_loop 批量），
/// 独立实现而非重构 watch_dir：02 命令工作正常，不做顺手重构（精准修改约束）。
/// @returns 须持活的监视句柄（drop 即停止）
fn start_theme_watch(
    dir: &Path,
    channel: Channel<Vec<WatchEvent>>,
) -> Result<notify::RecommendedWatcher, String> {
    let (tx, rx) = std::sync::mpsc::channel::<WatchEvent>();
    let watcher = watch_dir_inner(dir, move |ev| {
        // 合并器已断开（监视被替换、线程退出）后忽略
        let _ = tx.send(ev);
    })?;
    std::thread::spawn(move || {
        flush_loop(rx, move |batch| {
            // Channel 发送失败（前端已销毁）忽略：监视继续直到被替换
            let _ = channel.send(batch);
        });
    });
    Ok(watcher)
}

/// 命令：订阅主题目录变更（AC-T3-1/2 热刷新事件源）
///
/// 单槽替换：重复订阅替换旧句柄（旧句柄 drop 即停旧监视）——前端重订阅自愈。
/// 与文档监视（AppState.watcher 多槽）隔离：用户把 themes 目录当工作区打开时，
/// watch_dir/unwatch_dir 不影响主题监视。
/// 注：app 参数经 mock 运行时直呼会解析真实 %APPDATA%（D-6），命令层不直测。
#[tauri::command]
pub fn watch_themes<R: tauri::Runtime>(
    app: tauri::AppHandle<R>,
    channel: Channel<Vec<WatchEvent>>,
) -> Result<(), String> {
    let dir = themes_dir(&app)?;
    let watcher = start_theme_watch(&dir, channel)?;
    // State 为临时值须先 let 绑定再取锁（watch.rs 同款 E0716 规避）
    let state = app.state::<crate::AppState>();
    let mut guard = state
        .theme_watcher
        .lock()
        .map_err(|_| "主题监视状态锁损坏".to_string())?;
    *guard = Some(watcher);
    Ok(())
}
```

`src-tauri/src/lib.rs` AppState 增字段：

```rust
    /// 08 主题目录监视槽位（单槽：watch_themes 重复调用替换旧句柄即停旧监视；
    /// 与文档监视多槽 Map 隔离——用户把 themes 目录当工作区打开时互不干扰）
    pub theme_watcher: Mutex<Option<notify::RecommendedWatcher>>,
```

`manage(AppState {...})` 初始化追加 `theme_watcher: Mutex::new(None),`；两处 mock 测试构造（`watch.rs:305`、`search.rs:668`）各追加 `theme_watcher: std::sync::Mutex::new(std::option::Option::None),`；`generate_handler!` 末尾追加 `io::themes::watch_themes`（前项补逗号）。capabilities 追加 `"allow-watch-themes"`。

- [ ] **Step 4: 运行转绿 + 全量 Rust 门禁**

Run: `cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`
Expected: 全 PASS（watch.rs/search.rs 存量测试随 AppState 同步仍绿）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/io/themes.rs src-tauri/src/lib.rs src-tauri/src/io/watch.rs src-tauri/src/io/search.rs src-tauri/capabilities/default.json
git commit -m "feat(theme): watch_themes 热刷新命令——专用监视槽位与文档监视隔离+合并批量事件"
```

---

### Task 10: themeStore 热刷新订阅（防抖刷新 + dispose）

**复杂度：复杂**（判定依据：监听生命周期 + 跨层事件流（spec §4 热刷新事件核心），定时器防抖分支多）。

**Files:**
- Modify: `src/features/theme/theme-store.ts`
- Modify: `src/features/theme/theme-store.spec.ts`（追加用例）

**Interfaces:**
- Consumes: Task 6 `watchThemes`、Task 9 wire 契约
- Produces（Task 13/14 消费）:

```typescript
async function refresh(): Promise<void>   // 重扫目录 + 重挂链（AC-T3-1/2）
function dispose(): void                  // 清理防抖定时器（Task 13 追加色系退订）
// init 追加：watchThemes(scheduleRefresh) 订阅（失败降级重启可见）
```

- [ ] **Step 1: 写失败测试**

`theme-store.spec.ts` 追加 describe（use 补 `afterEach`）：

```typescript
describe("themeStore 热刷新（Task 10）", () => {
  beforeEach(() => {
    vi.useFakeTimers();
    setActivePinia(createPinia());
    document.head.innerHTML = "";
    document.documentElement.className = "";
    vi.clearAllMocks();
    mocks.listThemes.mockResolvedValue(LIST);
    mocks.loadSettings.mockResolvedValue(SETTINGS);
    mocks.updateSettings.mockImplementation(async (patch: { theme?: Partial<{ lightTheme: string; darkTheme: string }> }) => ({
      theme: { ...SETTINGS.theme, ...patch.theme },
    }));
    vi.spyOn(console, "error").mockImplementation(mocks.errorSpy);
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it("目录变更事件 → 300ms 防抖后重扫并更新列表（AC-T3-1 数据侧）", async () => {
    const store = useThemeStore();
    await store.init();
    expect(store.themes).toHaveLength(3);
    // 捕获 watchThemes 订阅的批量回调
    const [, args] = mocks.watchThemes.mock.calls[0] as [(events: unknown[]) => void];
    // 新主题落盘（模拟用户复制 css）→ 事件批量到达
    mocks.listThemes.mockResolvedValue({
      ...LIST,
      themes: [...LIST.themes, { name: "solar-amber", fileName: "solar-amber.css", label: "Solar Amber", hasUserCss: false }],
    });
    args([{ kind: "create", path: "C:\\t\\solar-amber.css" }]);
    await vi.advanceTimersByTimeAsync(300);
    expect(mocks.listThemes).toHaveBeenCalledTimes(2); // init 一次 + 刷新一次
    expect(store.themes.some((t) => t.name === "solar-amber")).toBe(true);
  });

  it("事件风暴合并为单次刷新（防抖尾沿重置）", async () => {
    const store = useThemeStore();
    await store.init();
    const [, args] = mocks.watchThemes.mock.calls[0] as [(events: unknown[]) => void];
    args([{ kind: "modify", path: "C:\\t\\a.css" }]);
    await vi.advanceTimersByTimeAsync(200);
    args([{ kind: "modify", path: "C:\\t\\b.css" }]);
    await vi.advanceTimersByTimeAsync(200); // 距上次事件 200ms < 300ms，未触发
    expect(mocks.listThemes).toHaveBeenCalledTimes(1);
    await vi.advanceTimersByTimeAsync(100); // 尾沿到点
    expect(mocks.listThemes).toHaveBeenCalledTimes(2);
  });

  it("修改激活主题 → link href ?t= 更新（AC-T3-2）", async () => {
    vi.spyOn(Date, "now").mockReturnValue(1_000);
    const store = useThemeStore();
    await store.init();
    const before = (document.getElementById("markwell-theme-link") as HTMLLinkElement).getAttribute("href");
    const [, args] = mocks.watchThemes.mock.calls[0] as [(events: unknown[]) => void];
    vi.spyOn(Date, "now").mockReturnValue(2_000);
    args([{ kind: "modify", path: `${LIST.dir}\\markwell-light.css` }]);
    await vi.advanceTimersByTimeAsync(300);
    const after = (document.getElementById("markwell-theme-link") as HTMLLinkElement).getAttribute("href");
    expect(after).not.toBe(before);
    expect(after).toContain("?t=2000");
  });

  it("dispose 清理待触发定时器（悬挂回调不再刷新）", async () => {
    const store = useThemeStore();
    await store.init();
    const [, args] = mocks.watchThemes.mock.calls[0] as [(events: unknown[]) => void];
    args([{ kind: "create", path: "C:\\t\\x.css" }]);
    store.dispose();
    await vi.advanceTimersByTimeAsync(1_000);
    expect(mocks.listThemes).toHaveBeenCalledTimes(1); // 仅 init 那次
  });

  it("watchThemes 订阅失败仅记录并降级（热刷新退化为重启可见——官方基线）", async () => {
    mocks.watchThemes.mockRejectedValueOnce(new Error("订阅失败"));
    const store = useThemeStore();
    await store.init();
    expect(mocks.errorSpy).toHaveBeenCalled();
    expect(document.getElementById("markwell-theme-link")).not.toBeNull(); // 首次注入不受影响
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/theme-store.spec.ts`
Expected: 新增 5 用例 FAIL（watchThemes 未被调用/refresh 不存在）

- [ ] **Step 3: 实现**

`theme-store.ts` 增量：①头部 `theme-io` 导入扩为 `{ allowThemeAssetDirectory, listThemes, watchThemes }`（watchThemes 首次消费）；②init 的 `applyCurrent();` 之后追加订阅：

```typescript
    // 热刷新订阅（AC-T3-1/2）：失败仅记录——降级为重启可见（官方基线行为，D-5）
    try {
      await watchThemes(scheduleRefresh);
    } catch (e) {
      console.error("[MarkWell] 主题热刷新订阅失败（降级为重启可见）", e);
    }
```

②`selectTheme` 之后追加：

```typescript
  /** 防抖定时器（目录事件风暴合并；dispose 清理防悬挂回调） */
  let refreshTimer: ReturnType<typeof setTimeout> | null = null;

  /** 目录事件 → 300ms 尾沿防抖刷新（Rust 侧另有 100ms 合并窗口，此处合并前端风暴） */
  function scheduleRefresh(): void {
    if (refreshTimer !== null) {
      clearTimeout(refreshTimer);
    }
    refreshTimer = setTimeout(() => {
      refreshTimer = null;
      void refresh();
    }, 300);
  }

  /** 重扫目录 + 重挂链（?t= 取当前时间戳穿透 WebView 缓存；AC-T3-1/2） */
  async function refresh(): Promise<void> {
    const list = await listThemes();
    themes.value = list.themes;
    hasBaseUserCss.value = list.hasBaseUserCss;
    applyCurrent();
  }

  /** 清理可释放资源（App 卸载/测试收尾调用；Rust 监视槽位随应用生命周期存活） */
  function dispose(): void {
    if (refreshTimer !== null) {
      clearTimeout(refreshTimer);
      refreshTimer = null;
    }
  }
```

③return 追加 `refresh, dispose,`。

- [ ] **Step 4: 运行转绿 + lint**

Run: `npx vitest run src/features/theme/theme-store.spec.ts && npx eslint src/features/theme --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/features/theme/theme-store.ts src/features/theme/theme-store.spec.ts
git commit -m "feat(theme): themeStore 热刷新——watch_themes 订阅+300ms 防抖重挂+dispose 清理"
```

---

### Task 11: Typora 变量映射兜底层

**复杂度：简单**（判定依据：1 个静态 CSS + main.ts 一行引入 + 文件钉桩测试，输入完整）。

**Files:**
- Create: `src/styles/typora-var-bridge.css`
- Modify: `src/main.ts`（追加静态引入）
- Create: `src/styles/typora-var-bridge.spec.ts`

**Interfaces:**
- Consumes: 官方常用变量集（依赖面核查 15）；crepe-overrides.css 亮色取值（回退值须与之一致）
- Produces: 无导出（全局 CSS 层；加载顺序 = crepe-overrides 之后、主题 link 之前）

- [ ] **Step 1: 写失败测试**

```typescript
// src/styles/typora-var-bridge.spec.ts
// Typora 变量映射兜底层钉桩（spec §3 兼容策略，尽力而为）：作用域/核心映射/
// 回退值形态/加载顺序。直读文件断言（mermaid-menu.spec 先例）。
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { describe, expect, it } from "vitest";

const css = readFileSync(resolve("src/styles/typora-var-bridge.css"), "utf8");
const mainTs = readFileSync(resolve("src/main.ts"), "utf8");

describe("Typora 变量映射兜底层", () => {
  it("作用域为 .milkdown 单选择器（特异性低于主题双选择器段，不抢主题层）", () => {
    expect(css).toMatch(/^\.milkdown \{/m);
    expect(css).not.toContain(".markwell-dark");
  });

  it("官方常用变量核心映射钉桩", () => {
    expect(css).toContain("--crepe-color-background: var(--bg-color,");
    expect(css).toContain("--crepe-color-on-background: var(--text-color,");
    expect(css).toContain("--crepe-color-primary: var(--primary-color,");
    expect(css).toContain("--crepe-color-outline: var(--border-color,");
    expect(css).toContain("--crepe-color-inline-code: var(--code-color,");
    expect(css).toContain("--crepe-color-surface-low: var(--side-bar-bg-color,");
  });

  it("全部映射行带字面量回退值（Typora 变量未定义时回落，与 crepe-overrides 亮色同值）", () => {
    const lines = css.split("\n").filter((l) => l.trim().startsWith("--crepe-"));
    expect(lines.length).toBeGreaterThan(10);
    for (const line of lines) {
      expect(line).toMatch(/var\(--[a-z-]+,\s*#\w+\)/);
    }
  });

  it("加载顺序：crepe-overrides（基础层）之后静态引入（主题 link 之前的映射层位置）", () => {
    const overrides = mainTs.indexOf("./styles/crepe-overrides.css");
    const bridge = mainTs.indexOf("./styles/typora-var-bridge.css");
    expect(overrides).toBeGreaterThan(-1);
    expect(bridge).toBeGreaterThan(overrides);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/styles/typora-var-bridge.spec.ts`
Expected: FAIL（文件不存在，readFileSync 抛错）

- [ ] **Step 3: 实现**

`src/styles/typora-var-bridge.css`：

```css
/* src/styles/typora-var-bridge.css
   08 主题：Typora 官方常用变量 → Crepe 变量映射兜底层（spec §3 兼容策略）。
   仅当用户 Typora 主题定义了对应变量时生效；回退值与 crepe-overrides.css 亮色段
   同值（两处取值须同步维护）。加载顺序：crepe-overrides（基础层）之后、主题 link
   （主题层）之前——原生主题自定义的 --crepe-* 在更晚的 link 层取胜，映射层不干扰。
   官方明言变量集可能变化，映射为尽力而为；DOM 结构差异（#write 等）不兼容属预期
   （spec §1 已披露）。 */
.milkdown {
  --crepe-color-background: var(--bg-color, #ffffff);
  --crepe-color-on-background: var(--text-color, #1f2937);
  --crepe-color-surface: var(--bg-color, #f8fafc);
  --crepe-color-surface-low: var(--side-bar-bg-color, #f1f5f9);
  --crepe-color-on-surface: var(--text-color, #1f2937);
  --crepe-color-on-surface-variant: var(--text-color, #475569);
  --crepe-color-outline: var(--border-color, #cbd5e1);
  --crepe-color-primary: var(--primary-color, #0f766e);
  --crepe-color-secondary: var(--select-text-bg-color, #ccfbf1);
  --crepe-color-on-secondary: var(--bg-color, #134e4a);
  --crepe-color-inverse: var(--text-color, #134e4a);
  --crepe-color-on-inverse: var(--bg-color, #f0fdfa);
  --crepe-color-inline-code: var(--code-color, #c2410c);
  --crepe-color-error: var(--error-color, #dc2626);
  --crepe-color-hover: var(--hover-bg-color, #f0fdfa);
  --crepe-color-selected: var(--select-text-bg-color, #ccfbf1);
  --crepe-color-inline-area: var(--bg-color, #e2e8f0);
}
```

`src/main.ts` 在 `import "./styles/crepe-overrides.css";` 之后追加：

```typescript
import "./styles/typora-var-bridge.css";
```

- [ ] **Step 4: 运行转绿 + lint**

Run: `npx vitest run src/styles/typora-var-bridge.spec.ts && npx eslint src/styles --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/styles/typora-var-bridge.css src/styles/typora-var-bridge.spec.ts src/main.ts
git commit -m "feat(theme): Typora 变量映射兜底层——官方常用变量到 Crepe 变量尽力转换"
```

---

### Task 12: color-scheme 系统色系监视模块

**复杂度：简单**（判定依据：单文件小模块纯封装，matchMedia 交互桩化后输入完整）。

**Files:**
- Create: `src/features/theme/color-scheme.ts`
- Create: `src/features/theme/color-scheme.spec.ts`

**Interfaces:**
- Consumes: `window.matchMedia`（jsdom 逐用例覆盖——setup.ts 已有恒 false 桩）
- Produces（Task 13 消费）:

```typescript
/** 当前系统是否暗色（matchMedia 首选色系即时求值） */
export function currentSystemDark(): boolean
/** 订阅系统色系变化（prefers-color-scheme）；返回退订函数 */
export function watchSystemColorScheme(onChange: (dark: boolean) => void): () => void
```

- [ ] **Step 1: 写失败测试**

```typescript
// src/features/theme/color-scheme.spec.ts
// 系统色系监视（08 spec T5 明暗源）：即时求值 + change 订阅 + 退订。
// setup.ts 的 matchMedia 恒 false 桩不满足本模块——逐用例覆盖可控桩。
import { afterEach, describe, expect, it, vi } from "vitest";
import { currentSystemDark, watchSystemColorScheme } from "./color-scheme";

type Listener = (event: { matches: boolean }) => void;

/** 安装可控 matchMedia 桩，返回触发器与监听计数 */
function installMatchMedia(initial: boolean) {
  const listeners = new Set<Listener>();
  const mql = {
    matches: initial,
    addEventListener: (_: string, l: Listener) => listeners.add(l),
    removeEventListener: (_: string, l: Listener) => listeners.delete(l),
  };
  const original = window.matchMedia;
  window.matchMedia = vi.fn(() => mql) as unknown as typeof window.matchMedia;
  return {
    fire(matches: boolean) {
      mql.matches = matches;
      listeners.forEach((l) => l({ matches }));
    },
    listenerCount: () => listeners.size,
    restore() {
      window.matchMedia = original;
    },
  };
}

describe("color-scheme 系统色系监视", () => {
  const handles: ReturnType<typeof installMatchMedia>[] = [];
  afterEach(() => {
    for (const h of handles.splice(0)) h.restore();
  });

  it("currentSystemDark 即时求值 true/false 两态", () => {
    const h = installMatchMedia(true);
    handles.push(h);
    expect(currentSystemDark()).toBe(true);
    h.fire(false);
    expect(currentSystemDark()).toBe(false);
  });

  it("订阅 change 回调携带最新色系（AC-T5-1 事件源）", () => {
    const h = installMatchMedia(false);
    handles.push(h);
    const onChange = vi.fn();
    watchSystemColorScheme(onChange);
    h.fire(true);
    expect(onChange).toHaveBeenCalledWith(true);
    h.fire(false);
    expect(onChange).toHaveBeenCalledWith(false);
  });

  it("退订后不再回调（监听泄漏防护）", () => {
    const h = installMatchMedia(false);
    handles.push(h);
    const onChange = vi.fn();
    const unsubscribe = watchSystemColorScheme(onChange);
    expect(h.listenerCount()).toBe(1);
    unsubscribe();
    expect(h.listenerCount()).toBe(0);
    h.fire(true);
    expect(onChange).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/color-scheme.spec.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```typescript
// 系统色系监视（08 spec T5 明暗源；D-3：系统跟随、不钉死窗口主题）
//
// matchMedia('(prefers-color-scheme: dark)') 为唯一事实源——WebView2 色系跟随
// 系统时 change 事件即系统明暗切换；主题内 media query 自适应同样依赖该链路
// （AC-T5-2，注入层不干预）。纯封装无状态；退订函数交调用方持有。
const DARK_QUERY = "(prefers-color-scheme: dark)";

/** 当前系统是否暗色 */
export function currentSystemDark(): boolean {
  return window.matchMedia(DARK_QUERY).matches;
}

/**
 * 订阅系统色系变化
 * @param onChange 色系变化回调（dark=true 暗色）
 * @returns 退订函数（dispose 链消费）
 */
export function watchSystemColorScheme(onChange: (dark: boolean) => void): () => void {
  const mql = window.matchMedia(DARK_QUERY);
  const handler = (event: MediaQueryListEvent): void => onChange(event.matches);
  mql.addEventListener("change", handler);
  return () => mql.removeEventListener("change", handler);
}
```

- [ ] **Step 4: 运行转绿 + lint**

Run: `npx vitest run src/features/theme/color-scheme.spec.ts && npx eslint src/features/theme --max-warnings 0`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/features/theme/color-scheme.ts src/features/theme/color-scheme.spec.ts
git commit -m "feat(theme): 系统色系监视模块——matchMedia 即时求值与 change 订阅"
```

---

### Task 13: themeStore 明暗联动与 App cleanup 接线

**复杂度：复杂**（判定依据：明暗切换链路是 spec §4 前端核心（AC-T5-1/2），跨 store/注入服务/根类三面 + App.vue 生命周期接线）。

**Files:**
- Modify: `src/features/theme/theme-store.ts`（接入 currentSystemDark/watchSystemColorScheme + dispose 扩展）
- Modify: `src/features/theme/theme-store.spec.ts`（追加用例）
- Modify: `src/features/theme/register.ts`（返回 cleanup 函数）
- Modify: `src/App.vue`（cleanup 挂接）

**Interfaces:**
- Consumes: Task 12 `currentSystemDark`/`watchSystemColorScheme`
- Produces:

```typescript
// register.ts 签名变更：export function registerThemeFeature(): () => void
// store 新增内部行为：init 读真实系统色系；色系变化 → systemDark 镜像 + applyCurrent()
// dispose 追加色系退订
```

- [ ] **Step 1: 写失败测试**

`theme-store.spec.ts`：mock 区追加 `currentDark: vi.fn(() => false)` 与 `watchScheme: vi.fn(() => () => {})`，并追加模块 mock：

```typescript
vi.mock("./color-scheme", () => ({
  currentSystemDark: mocks.currentDark,
  watchSystemColorScheme: mocks.watchScheme,
}));
```

追加 describe（基座 describe 的 beforeEach 已就绪公共 mock；此处复用）：

```typescript
describe("themeStore 明暗联动（Task 13）", () => {
  beforeEach(() => {
    setActivePinia(createPinia());
    document.head.innerHTML = "";
    document.documentElement.className = "";
    vi.clearAllMocks();
    mocks.listThemes.mockResolvedValue(LIST);
    mocks.loadSettings.mockResolvedValue(SETTINGS);
    mocks.updateSettings.mockImplementation(async (patch: { theme?: Partial<{ lightTheme: string; darkTheme: string }> }) => ({
      theme: { ...SETTINGS.theme, ...patch.theme },
    }));
    vi.spyOn(console, "error").mockImplementation(mocks.errorSpy);
    vi.spyOn(Date, "now").mockReturnValue(1_000);
  });

  it("init 读真实系统色系：暗色系统下启动即挂暗色主题并激活根类（AC-T5-1 初始态）", async () => {
    mocks.currentDark.mockReturnValue(true);
    const store = useThemeStore();
    await store.init();
    expect(store.systemDark).toBe(true);
    expect(document.documentElement.classList.contains("markwell-dark")).toBe(true);
    const link = document.getElementById("markwell-theme-link") as HTMLLinkElement;
    expect(link.getAttribute("href")).toContain("/markwell-dark.css?t=");
  });

  it("系统色系切换 → 对应主题自动应用与根类切换（AC-T5-1）", async () => {
    const store = useThemeStore();
    await store.init();
    // 捕获 watchSystemColorScheme 订阅的回调（Task 12 模块被 mock）
    const onChange = mocks.watchScheme.mock.calls[0][0] as (dark: boolean) => void;
    onChange(true);
    expect(document.documentElement.classList.contains("markwell-dark")).toBe(true);
    expect(
      (document.getElementById("markwell-theme-link") as HTMLLinkElement).getAttribute("href"),
    ).toContain("/markwell-dark.css?t=");
    onChange(false);
    expect(document.documentElement.classList.contains("markwell-dark")).toBe(false);
    expect(
      (document.getElementById("markwell-theme-link") as HTMLLinkElement).getAttribute("href"),
    ).toContain("/markwell-light.css?t=");
  });

  it("暗色模式下明暗主题分设生效（设置值失效回落内置暗色）", async () => {
    mocks.currentDark.mockReturnValue(true);
    const store = useThemeStore();
    await store.init();
    store.darkTheme = "deleted-dark"; // 设置值失效场景
    expect(store.resolveActiveTheme("dark")?.name).toBe("markwell-dark"); // 内置兜底
  });

  it("dispose 退订系统色系监听（App 卸载链路）", async () => {
    const store = useThemeStore();
    await store.init();
    expect(mocks.watchScheme).toHaveBeenCalledTimes(1);
    const unsubscribe = mocks.watchScheme.mock.results[0].value as () => void;
    store.dispose();
    expect(unsubscribe).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/theme-store.spec.ts`
Expected: 新增 4 用例 FAIL（systemDark 恒 false / watchScheme 未被调用）

- [ ] **Step 3: 实现**

`theme-store.ts` 增量：

①头部 use 调整：

```typescript
import { applyThemeCss } from "./theme-css";
import { currentSystemDark, watchSystemColorScheme } from "./color-scheme";
```

②模块级（defineStore 外）不新增；store 内部追加退订持有变量（`refreshTimer` 声明附近）：

```typescript
  /** 系统色系退订函数（init 订阅、dispose 释放；null=未订阅） */
  let stopColorScheme: (() => void) | null = null;
```

③`init` 中 `assetBase.value = ...` 之后追加：

```typescript
    systemDark.value = currentSystemDark();
    // 订阅系统色系（AC-T5-1）：切换即镜像 + 重挂对应主题（明暗分离选择，Task 5 存储）
    stopColorScheme = watchSystemColorScheme((dark) => {
      systemDark.value = dark;
      applyCurrent();
    });
```

④`dispose` 追加：

```typescript
    stopColorScheme?.();
    stopColorScheme = null;
```

`register.ts` 改为返回 cleanup：

```typescript
// 主题功能装配（08；App.vue onMounted 调一次。12 窗口外壳后续接管菜单接线）
import { useThemeStore } from "./theme-store";

/**
 * 注册主题功能：初始化主题 store（读设置 + 扫描目录 + asset 授权 + 系统色系订阅 +
 * 首次注入）。异步流程不阻塞启动；失败经 store 内日志降级。
 * @returns 注销函数（App 卸载调用：退订色系监听 + 清理防抖定时器）
 */
export function registerThemeFeature(): () => void {
  const store = useThemeStore();
  void store.init();
  return () => store.dispose();
}
```

`App.vue`：`registerThemeFeature();` 一行改为 `const cleanupThemeFeature = registerThemeFeature();`（附注释「08 主题装配（cleanup 含色系退订）」）；`onBeforeUnmount` 内追加 `cleanupThemeFeature();`。

- [ ] **Step 4: 运行转绿 + 类型检查**

Run: `npx vitest run src/features/theme/theme-store.spec.ts && npm run typecheck`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/features/theme/theme-store.ts src/features/theme/theme-store.spec.ts src/features/theme/register.ts src/App.vue
git commit -m "feat(theme): 明暗分离联动——系统色系订阅切主题+.markwell-dark 根类激活+App cleanup"
```

---

### Task 14: DevTools 全链路（Rust toggle_devtools + Shift+F12 接线）

**复杂度：复杂**（判定依据：跨 IPC + cfg 门控编译面 + Cargo feature + 前端快捷键接线，多文件）。

**Files:**
- Create: `src-tauri/src/devtools.rs`
- Modify: `src-tauri/src/lib.rs`（`pub mod devtools;` + 注册）
- Modify: `src-tauri/Cargo.toml`（`[features] devtools`）
- Modify: `src-tauri/capabilities/default.json`（`allow-toggle-devtools`）
- Create: `src/features/theme/devtools.ts`
- Create: `src/features/theme/devtools.spec.ts`
- Modify: `src/features/theme/register.ts`（注册快捷键）

**Interfaces:**
- Consumes: tauri WebviewWindow devtools 方法（`#[cfg(any(debug_assertions, feature = "devtools"))]` 门控，依赖面核查 18）；Task 6 `toggleDevtools`
- Produces:

```rust
/// 命令：切换 DevTools 开合；@returns true=已打开 false=已关闭（或构建不支持）
#[tauri::command]
pub fn toggle_devtools(window: tauri::WebviewWindow) -> Result<bool, String>
```

```typescript
// devtools.ts
/** 注册 Shift+F12 窗口级快捷键（View → Toggle DevTools；12 可接管菜单装配）；返回注销函数 */
export function registerDevtoolsShortcut(): () => void
```

- [ ] **Step 1: 写失败测试（前端接线；Rust 命令层 cfg 门控无 mock 断言面——以双分支编译验证兜底，披露）**

```typescript
// src/features/theme/devtools.spec.ts
// Shift+F12 窗口级快捷键（08 T7；app-shortcuts.spec 同款 KeyboardEvent 派发手法）
import { beforeEach, describe, expect, it, vi } from "vitest";

const mocks = vi.hoisted(() => ({ toggle: vi.fn() }));
vi.mock("../../services/theme-io", () => ({ toggleDevtools: mocks.toggle }));

import { registerDevtoolsShortcut } from "./devtools";

describe("Shift+F12 DevTools 快捷键（AC-T7-1 前端段）", () => {
  beforeEach(() => {
    mocks.toggle.mockReset();
    vi.spyOn(console, "error").mockImplementation(() => {});
  });

  it("Shift+F12 触发 DevTools 切换命令并阻止默认", () => {
    mocks.toggle.mockResolvedValue(true);
    const cleanup = registerDevtoolsShortcut();
    const ev = new KeyboardEvent("keydown", { key: "F12", shiftKey: true, cancelable: true });
    const prevented = !window.dispatchEvent(ev);
    expect(mocks.toggle).toHaveBeenCalledTimes(1);
    expect(prevented).toBe(true);
    cleanup();
  });

  it("无 Shift 修饰的 F12 不触发（留给浏览器默认行为）", () => {
    const cleanup = registerDevtoolsShortcut();
    window.dispatchEvent(new KeyboardEvent("keydown", { key: "F12" }));
    expect(mocks.toggle).not.toHaveBeenCalled();
    cleanup();
  });

  it("命令失败仅记录错误不崩溃", async () => {
    mocks.toggle.mockRejectedValueOnce(new Error("构建不支持"));
    const cleanup = registerDevtoolsShortcut();
    window.dispatchEvent(new KeyboardEvent("keydown", { key: "F12", shiftKey: true }));
    await vi.waitFor(() => expect(console.error).toHaveBeenCalled());
    cleanup();
  });

  it("注销后不再触发", () => {
    const cleanup = registerDevtoolsShortcut();
    cleanup();
    window.dispatchEvent(new KeyboardEvent("keydown", { key: "F12", shiftKey: true }));
    expect(mocks.toggle).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/features/theme/devtools.spec.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`src-tauri/src/devtools.rs`：

```rust
// DevTools 开关（08 T7；View 菜单装配后续归 12 窗口外壳）
//
// 编译门控：tauri 的 open/close/is_devtools_open 方法带
// #[cfg(any(debug_assertions, feature = "devtools"))]——debug 构建默认可用，
// release 须经本 crate 的 devtools feature（映射 tauri/devtools）显式启用；
// 未启用构建下命令退化为无操作返回 false（前端快捷键不报错）。

/// 切换 DevTools 开合（AC-T7-1；Shift+F12 由前端窗口级快捷键触发本命令）
///
/// @returns 切换后状态：true=已打开，false=已关闭（或当前构建不支持 DevTools）
#[tauri::command]
pub fn toggle_devtools(window: tauri::WebviewWindow) -> Result<bool, String> {
    #[cfg(any(debug_assertions, feature = "devtools"))]
    {
        if window.is_devtools_open() {
            window.close_devtools();
            Ok(false)
        } else {
            window.open_devtools();
            Ok(true)
        }
    }
    #[cfg(not(any(debug_assertions, feature = "devtools")))]
    {
        // 未启用构建：显式消费参数避免 unused 警告；无操作返回 false
        let _ = window;
        Ok(false)
    }
}
```

`src-tauri/src/lib.rs`：`pub mod io;` 旁追加 `pub mod devtools;`；`generate_handler!` 末尾追加 `devtools::toggle_devtools`（前项补逗号）。

`src-tauri/Cargo.toml` 追加：

```toml
[features]
# 08 T7 DevTools：release 构建显式启用（debug 构建默认编译 devtools 能力；
# tauri 的 devtools 方法带 #[cfg(any(debug_assertions, feature = "devtools"))] 门控）
devtools = ["tauri/devtools"]
```

`src-tauri/capabilities/default.json` 追加 `"allow-toggle-devtools"`。

`src/features/theme/devtools.ts`：

```typescript
// DevTools 快捷键（08 T7）：Shift+F12 窗口级组合（编辑器内快捷键归 01 keymap 注册表，
// 本层只处理窗口级组合——app-shortcuts.ts 同一分层）。命令本体见 Rust devtools.rs，
// debug 构建默认可用、release 需 devtools feature（不支持时命令返回 false 不报错）。
import { toggleDevtools } from "../../services/theme-io";

/**
 * 注册 Shift+F12 窗口级快捷键（View → Toggle DevTools；12 窗口外壳可整体接管）
 * @returns 注销函数（App 卸载调用）
 */
export function registerDevtoolsShortcut(): () => void {
  const onKeydown = (event: KeyboardEvent): void => {
    // 仅 Shift+F12 命中；无 Shift 的 F12 留给浏览器默认行为
    if (!(event.shiftKey && event.key === "F12")) return;
    event.preventDefault();
    // 开合结果无需消费；失败（构建不支持/IPC 异常）仅记录不打断交互
    void toggleDevtools().catch((e: unknown) => {
      console.error("[MarkWell] DevTools 切换失败", e);
    });
  };
  window.addEventListener("keydown", onKeydown);
  return () => window.removeEventListener("keydown", onKeydown);
}
```

`src/features/theme/register.ts`：

```typescript
// 主题功能装配（08；App.vue onMounted 调一次。12 窗口外壳后续接管菜单接线）
import { useThemeStore } from "./theme-store";
import { registerDevtoolsShortcut } from "./devtools";

/**
 * 注册主题功能：初始化主题 store（读设置 + 扫描目录 + asset 授权 + 系统色系订阅 +
 * 首次注入）+ Shift+F12 DevTools 快捷键。异步流程不阻塞启动；失败经日志降级。
 * @returns 注销函数（App 卸载调用：注销快捷键 + 退订色系监听 + 清理定时器）
 */
export function registerThemeFeature(): () => void {
  const store = useThemeStore();
  void store.init();
  const cleanupDevtools = registerDevtoolsShortcut();
  return () => {
    cleanupDevtools();
    store.dispose();
  };
}
```

- [ ] **Step 4: 运行转绿 + 双分支编译验证 + 全量 Rust 门禁**

Run: `npx vitest run src/features/theme/devtools.spec.ts && npm run typecheck && cargo check --manifest-path src-tauri/Cargo.toml --features devtools && cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: 全 PASS（`--features devtools` 验证 release 启用分支可编译——AC-T7-1 的 release 面证据）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/devtools.rs src-tauri/src/lib.rs src-tauri/Cargo.toml src-tauri/capabilities/default.json src/features/theme/devtools.ts src/features/theme/devtools.spec.ts src/features/theme/register.ts
git commit -m "feat(theme): DevTools 开关——toggle_devtools cfg 门控+Shift+F12 窗口级快捷键"
```

---

### Task 15: 字体与 rem 字号验证加固

**复杂度：简单**（判定依据：纯测试加固任务——T8-1/T8-2 的可自动化证据面补全，无生产代码变更）。

**Files:**
- Create: `src/features/theme/builtin-theme.spec.ts`
- Modify: `src/features/theme/theme-css.spec.ts`（追加目录基准用例）

**Interfaces:**
- Consumes: Task 3 CSS 资产（`src-tauri/assets/themes/*.css`）、Task 7 `applyThemeCss`
- Produces: AC-T8-1/AC-T8-2 的可自动化证据（组合证据 + 钉桩，披露口径见附录 A）

- [ ] **Step 1: 写失败测试（先写断言再核对资产——若资产不满足即 RED）**

`src/features/theme/builtin-theme.spec.ts`：

```typescript
// src/features/theme/builtin-theme.spec.ts
// 内置主题钉桩（08 T1/T8）：Crepe 变量全集、rem 字号基准、双选择器作用域。
// 直读资产文件断言（mermaid-menu.spec 先例）；防库/资产升级静默漂移。
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { describe, expect, it } from "vitest";

/** Crepe 22 变量体系全集（17 色 + 3 字体 + 2 阴影，另含 base-font-size 基准字号） */
const CREPE_VARS = [
  "--crepe-color-background",
  "--crepe-color-on-background",
  "--crepe-color-surface",
  "--crepe-color-surface-low",
  "--crepe-color-on-surface",
  "--crepe-color-on-surface-variant",
  "--crepe-color-outline",
  "--crepe-color-primary",
  "--crepe-color-secondary",
  "--crepe-color-on-secondary",
  "--crepe-color-inverse",
  "--crepe-color-on-inverse",
  "--crepe-color-inline-code",
  "--crepe-color-error",
  "--crepe-color-hover",
  "--crepe-color-selected",
  "--crepe-color-inline-area",
  "--crepe-base-font-size",
  "--crepe-font-title",
  "--crepe-font-default",
  "--crepe-font-code",
  "--crepe-shadow-1",
  "--crepe-shadow-2",
];

const THEMES = ["markwell-light.css", "markwell-dark.css"] as const;

describe("内置主题资产钉桩", () => {
  for (const file of THEMES) {
    const css = readFileSync(resolve("src-tauri/assets/themes", file), "utf8");

    it(`${file} 覆盖 Crepe 变量全集（22 变量体系）`, () => {
      for (const name of CREPE_VARS) {
        expect(css).toContain(name);
      }
    });

    it(`${file} 基准字号为 rem（AC-T8-2：字号偏好调整时全文字号跟随的前提）`, () => {
      expect(css).toMatch(/--crepe-base-font-size:\s*1rem/);
    });

    it(`${file} 无 px 字号声明（rem 链路完整性）`, () => {
      expect(css).not.toMatch(/font-size:\s*\d+(\.\d+)?px/);
    });

    it(`${file} 采用双选择器作用域（D-4：主题段须能覆盖 crepe-overrides 暗色默认段）`, () => {
      expect(css).toMatch(/^\.milkdown,\s*\n\.markwell-dark \.milkdown \{/m);
    });

    it(`${file} 无外部 @import（离线完整性，主题经 asset 协议加载）`, () => {
      expect(css).not.toMatch(/@import/);
    });
  }

  it("亮暗两套主题内容独立（非复制品）", () => {
    const light = readFileSync(resolve("src-tauri/assets/themes/markwell-light.css"), "utf8");
    const dark = readFileSync(resolve("src-tauri/assets/themes/markwell-dark.css"), "utf8");
    expect(light).not.toBe(dark);
    expect(light).toContain("#ffffff"); // 亮色底
    expect(dark).toContain("#101418"); // 暗色底
  });
});
```

`theme-css.spec.ts` 追加用例（AC-T8-1 的注入侧证据）：

```typescript
  it("href 以目录基准拼接——主题 CSS 内相对 url(./fonts/x) 以主题目录解析（AC-T8-1）", () => {
    // 浏览器语义：<link href="<base>/theme.css"> 内的相对 url() 以 base 目录为基准；
    // 本断言钉住「link href = 主题目录/文件名」形态（fonts/ 子目录可达的前提），
    // 子目录路径 + 尾随分隔符下亦不得出现双斜杠
    applyThemeCss({
      themesAssetBase: "http://asset.localhost/C%3A%5Cthemes%5Csub/",
      theme: light,
      hasBaseUserCss: false,
      cacheBust: 3,
    });
    const href = hrefOf(THEME_LINK_ID);
    expect(href).toBe("http://asset.localhost/C%3A%5Cthemes%5Csub/markwell-light.css?t=3");
    expect(href).not.toContain("//markwell"); // 无重复分隔符
  });
```

- [ ] **Step 2: 运行确认结果**

Run: `npx vitest run src/features/theme/builtin-theme.spec.ts src/features/theme/theme-css.spec.ts`
Expected: PASS（Task 3/7 的资产与实现已满足断言；若 FAIL 则回修资产/实现——本任务即 T8 验收门）

- [ ] **Step 3: 提交**

```bash
git add src/features/theme/builtin-theme.spec.ts src/features/theme/theme-css.spec.ts
git commit -m "test(theme): 字体与 rem 字号验证加固——Crepe 变量全集/双选择器/href 目录基准钉桩"
```

---

### Task 16: 覆盖率阈值专项（主题核心域 glob）

**复杂度：简单**（判定依据：单文件配置追加 + 全量验证，glob 键结构照抄既有条目）。

**Files:**
- Modify: `vite.config.ts`（thresholds 追加 4 个 glob）

**Interfaces:**
- Consumes: 主题前端核心域 4 文件（Task 6/7/8/12 产物，均已达 100% 分支）
- Produces: 覆盖率门禁对主题核心域的 100% 约束（宪法 A.6.2：新增核心域必须同步 thresholds）

- [ ] **Step 1: 修改 vite.config.ts**

`coverage.thresholds` 内 `src/features/image/upload-flow.ts` 条目之后追加（照抄既有条目结构，勿动其他配置）：

```typescript
        // 08 主题核心 100%（spec §4 前端核心：明暗切换链路 / CSS 4 层注入顺序 /
        // 热刷新事件；含主题域 IPC 契约封装与系统色系监视。register/devtools/
        // builtin-theme 钉桩等装配与验证层走全局 80% + E2E）
        "src/features/theme/theme-store.ts": {
          lines: 100,
          statements: 100,
          functions: 100,
          branches: 100,
        },
        "src/features/theme/theme-css.ts": {
          lines: 100,
          statements: 100,
          functions: 100,
          branches: 100,
        },
        "src/features/theme/color-scheme.ts": {
          lines: 100,
          statements: 100,
          functions: 100,
          branches: 100,
        },
        "src/services/theme-io.ts": {
          lines: 100,
          statements: 100,
          functions: 100,
          branches: 100,
        },
```

- [ ] **Step 2: 全量覆盖率门禁验证**

先按宪法 C.6.2 甄别残留 vitest 进程（`wmic process where "name='node.exe'" get processid,commandline` 甄别 CommandLine，严禁误杀其他项目进程）。

Run: `npm run test:coverage`
Expected: 全绿——全局四项 ≥80%，主题 4 glob 均 100%，既有 20 个 glob 不回退。若有未覆盖分支，回对应任务补测试后重跑（禁止降阈值）。

- [ ] **Step 3: 提交**

```bash
git add vite.config.ts
git commit -m "test(theme): 覆盖率阈值新增主题核心域——状态机/注入服务/色系监视/IPC 封装 100%"
```

---

### Task 17: README 同步（用户可见变更）

**复杂度：简单**（判定依据：单文件文档更新；gitignored 本地文档，不入库——依赖面核查 19）。

**Files:**
- Modify: **主仓** `D:\code\project\typora\README.md`（本地 gitignored 文档，现为 7 行 Tauri 模板；worktree 内无该文件。主控裁决 2026-09-05：README 不入库——沿 1a44a76 用户裁决，且主仓已有同名未跟踪文件，入库会致合入后 `git pull` 拒绝覆盖；宪法 B.5.6 以工作树同步满足并列 PR 披露）

**Interfaces:**
- Consumes: 全部已交付功能行为
- Produces: 用户可见变更的文档说明（宪法 B.5.6）

- [ ] **Step 1: 扩充主仓 README.md**

保留现有 7 行模板内容（标题 + Recommended IDE Setup 节），在其后追加以下「主题」节（不得删除既有内容）：

```markdown
## 功能说明：主题（08 模块）

- **内置主题**：`Markwell Light`（亮色）/ `Markwell Dark`（暗色），原创编写，基于 Milkdown Crepe 的 22 个 `--crepe-*` 变量。
- **明暗联动**：亮/暗模式分别记忆所选主题，跟随系统明暗自动切换；主题内 `prefers-color-scheme` media query 自适应支持。
- **主题目录**：`%APPDATA%\com.markwell.app\themes`。启动时自动预置内置主题；放入新 `.css` 无需重启即出现在主题列表（热刷新）。
- **命名规则**：仅小写字母与连字符（如 `my-first-theme.css`）；数字与大写不安装（对齐官方规则）。菜单标签 = 连字符分词后每词首字母大写（`My First Theme`）。
- **主题编写约定**：变量作用域写作 `.milkdown, .markwell-dark .milkdown { ... }`；字号一律用 rem（配合字号偏好）。
- **自定义追加 CSS**：`base.user.css` 对所有主题生效；`<主题名>.user.css` 仅对应主题生效（文件名大小写敏感）。加载顺序：基础样式 → 主题 CSS → base.user.css → {theme}.user.css。
- **自定义字体**：字体文件放主题目录 `fonts/` 子目录，主题 CSS 内以相对路径引用（如 `url(./fonts/x.woff2)`，相对基准 = 主题目录；建议文件名用小写字母与连字符）。
- **Typora 主题兼容**：官方常用变量（`--bg-color`/`--text-color`/`--primary-color` 等）经映射层尽力转换；DOM 结构差异导致的还原度损失属预期。
- **DevTools**：`Shift+F12` 开合（debug 构建默认可用；release 构建需启用 `devtools` cargo feature）。
```

- [ ] **Step 2: 验证（不做 git 提交——gitignored 文件，git 历史留痕由进度文件承担）**

```bash
grep -c "功能说明：主题" /d/code/project/typora/README.md
# 预期输出 1
git -C /d/code/project/typora status --short README.md
# 预期输出为空（gitignored 不入库）
```

- [ ] **Step 3: 无额外验证（文档任务；内容正确性由本计划附录 A 披露清单核对）**

---

### Task 18: E2E 核定链路（theme.e2e.ts）

**复杂度：复杂**（判定依据：真实应用进程 + 真实用户数据目录读写与还原 + 跨进程时序等待，生命周期敏感）。

**Files:**
- Create: `e2e/specs/theme.e2e.ts`

**Interfaces:**
- Consumes: 全部已交付链路（预置/注入/热刷新）；wdio 既有形态（smoke.e2e.ts 先例）；真实主题目录 `%APPDATA%\com.markwell.app\themes`
- Produces: E2E 核定链路证据——启动注入 + 真实生效 + 热刷新重挂（菜单 UI 链路按依赖面降级，见附录 A）

- [ ] **Step 1: 写 E2E 用例**

```typescript
import { $, browser, expect } from "@wdio/globals";
import { readFileSync, writeFileSync } from "node:fs";
import path from "node:path";

/**
 * 08 主题 E2E（计划核定链路：启动注入 → 变量真实生效 → 热刷新重挂）。
 * 菜单链路（AC-T1-1 菜单展示 / AC-T2-1 按钮触发 / AC-T5-1 系统切换）依赖
 * 10/12 模块 UI，未落地——验证层级降级 Vitest（PR 披露清单附录 A）。
 * 前置：①Rust 侧变更后先 cargo build（application 指 debug 产物）；
 *      ②本地系统为亮色（默认激活 markwell-light；CI 不跑 wdio——C.5 冒烟为进程级）；
 *      ③用例写真实主题目录（identifier com.markwell.app），finally 必须还原。
 */
// 真实主题目录（应用数据目录）
const themesDir = path.join(process.env.APPDATA ?? "", "com.markwell.app", "themes");
const lightCssPath = path.join(themesDir, "markwell-light.css");

/** 从主题 CSS 文本提取 --crepe-color-background 值（热刷新哨兵比对用） */
function backgroundOf(css: string): string {
  const m = css.match(/--crepe-color-background:\s*(#[0-9a-fA-F]{6})/);
  if (!m) throw new Error("主题文件缺少 --crepe-color-background");
  return m[1];
}

/** 读编辑器根上当前生效的背景变量（主题真实生效的直接证据） */
async function computedBackground(): Promise<string> {
  return browser.execute(() =>
    getComputedStyle(document.querySelector(".milkdown")!)
      .getPropertyValue("--crepe-color-background")
      .trim(),
  );
}

describe("08 主题链路", () => {
  it("启动后注入内置亮色主题且变量真实生效（AC-T1-2 服务端段实证）", async () => {
    await $(".milkdown"); // 等前端完全加载（编辑器挂载）
    const link = await $("#markwell-theme-link");
    await expect(link).toExist();
    const href = await link.getAttribute("href");
    expect(href).toContain("markwell-light.css"); // 默认亮色主题（settings 默认值）
    expect(href).toMatch(/^http:\/\/asset\.localhost\//); // asset 协议（CSP 已放行）
    expect(href).toContain("?t="); // 注入即带缓存戳
    // 内置主题已预置到真实数据目录（setup 钩子产物，D-1）
    const css = readFileSync(lightCssPath, "utf8");
    // 主题变量真实生效：计算值与主题文件定义一致（非 crepe-overrides 兜底态推测）
    expect(await computedBackground()).toBe(backgroundOf(css));
  });

  it("热刷新：修改激活主题文件 → link 以新 ?t= 重挂且变量更新（AC-T3-2）", async () => {
    const link = await $("#markwell-theme-link");
    const initialHref = await link.getAttribute("href");
    const original = readFileSync(lightCssPath, "utf8");
    try {
      // 哨兵色与默认 #ffffff 可区分且满足 #rrggbb 形态
      const sentinel = "#010203";
      const modified = original.replace(
        /--crepe-color-background:\s*#[0-9a-fA-F]{6}/,
        `--crepe-color-background: ${sentinel}`,
      );
      writeFileSync(lightCssPath, modified, "utf8");
      // watch_themes(100ms 合并) → 前端 300ms 防抖 → link 重挂，8s 上限留足时序余量
      await browser.waitUntil(
        async () => (await $("#markwell-theme-link").getAttribute("href")) !== initialHref,
        { timeout: 8000, timeoutMsg: "热刷新未重挂主题 link" },
      );
      expect(await link.getAttribute("href")).not.toBe(initialHref);
      // 样式真实更新（非仅 href 变化）
      await browser.waitUntil(async () => (await computedBackground()) === sentinel, {
        timeout: 5000,
        timeoutMsg: "主题变量未随热刷新更新",
      });
    } finally {
      // 还原内置主题文件（预置逻辑「缺失才写」，必须由测试自行恢复）
      writeFileSync(lightCssPath, original, "utf8");
    }
  });
});
```

- [ ] **Step 2: 构建并运行**

先按宪法 C.6.2 甄别残留 vitest/edgedriver 进程；随后：

Run: `cargo build --manifest-path src-tauri/Cargo.toml && npm run test:e2e`
Expected: theme.e2e.ts 两用例 PASS，既有 7 用例不回退

- [ ] **Step 3: 提交**

```bash
git add e2e/specs/theme.e2e.ts
git commit -m "test(theme): E2E 核定链路——启动注入/变量真实生效/热刷新重挂"
```

---

## 附录 A：PR 强制披露清单（汇总，随 PR 描述提交）

1. **依赖 10/12 未落地 UI 的 AC 验证层级降级**：AC-T1-1（菜单展示段→Vitest store.themes）、AC-T2-1（按钮触发→服务层契约测试）、AC-T3-1（菜单出现段→store.themes 更新测试）、AC-T5-1（设置入口→matchMedia 联动 Vitest）——菜单/设置页装配归 12/10 模块，数据与动作面本模块已交付并验证（详见「降级清单」节）。
2. **真实系统色系切换不可 E2E**（AC-T5-1/AC-T5-2）：jsdom 与 WebDriver 均无法模拟系统明暗切换；机制 = 系统跟随（D-3，不钉死窗口主题）+ 主题 media query 原生求值（D-9）。WebView2 色系跟随行为列 P5 手动验证项，若不成立的补救路径 = 后续接 `onThemeChanged` 双通道。
3. **AC-T7-1 DevTools 可视断言无自动化**：Vitest 验证快捷键接线，Rust 侧以 `cargo check --features devtools` 验证 release 分支编译；开合效果需 debug 构建手动确认。
4. **AC-T8-1 组合证据**：字体加载 = 授权递归（`allow_asset_directory(themesDir)` 含 fonts/）+ CSP font-src 放行 + link 相对解析（浏览器内建语义），无端到端字体渲染断言（真实 woff2 资产不在仓库）。
5. **AC-T8-2 字号偏好联动**：rem 基准已钉桩；偏好设置 UI 与联动链路待 10 模块落地后 E2E 复验。
6. **E2E 前提与范围**：本地系统亮色 + `cargo build` 先行；CI 不跑 wdio（C.5 进程级冒烟）；用例写真实用户主题目录并在 finally 还原。
7. **命令层 mock 直呼豁免**（D-6）：mock 运行时 identifier 为空、`app_data_dir()` 解析真实 %APPDATA%——`list_themes`/`watch_themes`/`open_theme_folder` 不直测，核心函数显式 dir 参数 100% 单测 + DTO 序列化钉桩 + E2E 实证 wire（偏离 watch.rs 先例的原因如上）。
8. **notify 8.2 静默 unwatch 风险接受**（D-5）：主题目录低频小目录，完整存活检测不做；热刷新订阅失败降级 = 重启可见（官方基线）。
9. **09 导出消费面未预置**：spec §5 提及 09 导出消费主题 CSS 文本——`read_file` 已可读主题文件，具体消费形态待 09 落地时按需扩展。
10. **typora-var-bridge 回退值与 crepe-overrides 亮色段同值**：两处取值需同步维护（文件头注释已声明）；官方变量集可能变化，映射为尽力而为。
11. **README 为 gitignored 本地文档**（1a44a76 用户裁决）：Task 17 更新不入库，留痕以本计划与进度文件为准。

## 附录 B：风险与验证边界

| # | 风险/边界 | 缓解 |
| --- | --- | --- |
| R1 | WebView2 PreferredColorScheme 未随系统更新（matchMedia 不触发） | P5 手动验证；补救 = onThemeChanged 双通道（后续变更，需再评审） |
| R2 | E2E 写真实主题目录中途失败 | finally 还原 + 用例只改 markwell-light.css 单文件 |
| R3 | 用户主题引用了不存在的 fonts/ 或 url | 浏览器自然降级（样式缺字体/图片破图），不崩溃；README 已给约定 |
| R4 | `theme=` 设置组存量用户（无 theme 键） | loadSettings 逐键回落默认内置主题（Task 5 用例覆盖） |
| R5 | themes 目录被当作工作区打开（文档监视同路径） | 主题监视独立槽位（Task 9），两路互不干扰 |
| R6 | 内置主题被用户删除后重启 | setup 钩子缺失即重新预置（D-1「缺失才写」语义的自然结果） |

## 附录 C：计划自审三查结论（P1 门禁留痕）

1. **spec coverage**：18 条 AC 全部映射到任务与测试层级（见映射表，无空缺）；spec §1.1 范围 11 项全部承接（映射表下方核对行）；spec §4 测试策略三面（Rust 单测/Vitest/E2E）分别由 Task 1-3/9、Task 7/10/12/13、Task 18 承接；spec §6 三阶段与批次 B1-B7 对齐（批次不跨阶段）。
2. **placeholder 扫描**：无 TBD/待定/「稍后实现」；全部任务含完整测试与实现代码块；唯一 TODO 为全局规范允许的预留接口标注（openThemeFolder → 10/12），符合「明确标注用于未来扩展」例外格式。
3. **类型一致性**：Rust `ThemeDto{name,file_name,label,has_user_css}` ↔ TS `ThemeMeta{name,fileName,label,hasUserCss}`（serde camelCase 钉桩测试 Task 2）；`ThemeListDto{dir,themes,has_base_user_css}` ↔ `ThemeListResult{dir,themes,hasBaseUserCss}`；`ActiveThemeRefs.theme` 用 `Pick<ThemeMeta,...>` 与消费端一致；themeStore 对外签名（init/selectTheme/resolveActiveTheme/refresh/dispose）在 Task 8/10/13 间逐字一致；`registerThemeFeature` 返回类型变更（void → () => void）在 Task 13 一次性完成且 App.vue 同步；`applyThemeCss(refs, doc?)` 各调用点一致；`start_theme_watch`/`watch_themes` 签名与 watch.rs 复用件一致。
