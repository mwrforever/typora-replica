# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)

- workspace_dir: D:\code\project\typora
- saved_at: 2026-08-15T22:20:00+08:00
- 版本控制提交号: b43fe1b（Task 18 收官提交；分支 feat/02-document-management 共 22 个提交）
- 任务一句话描述: **02 文档管理模块已全部 18 个任务实现完毕（Task 18 收官：E2E 冒烟扩展 6/6 全绿 + 全量门禁全绿 + 进度文档），28/28 AC 覆盖；下一阶段为 03 侧栏/目录树模块**

## 1. 已完成 (Completed) — 简要标记

- [x] 02 模块 18 个任务全部实现（分支 feat/02-document-management，22 提交）：文件 IO 基座（编码探测/BOM 剥离/GBK 回退/原子写/路径逃逸拒绝/行尾归一）、目录遍历与自然序排序、文档会话（打开/保存/另存/dirty 状态机/父目录加载/lastFile 持久化）、事件桥订阅接入（subscribeMarkdownUpdated + 并发守卫）、启动行为（get_cli_args/四模式决策/--new 优先/--reopen-file/回退新建提示）、最近文件（上限 10/置顶去重/固定项保留/失败移除）、自动保存（停笔 1s∪定时 5min/开关联动/dirty 协作）、草稿备份与恢复（5s 心跳/退出备份/首标题命名/空内容防护）、应用启动装配（Ctrl+S/Ctrl+O 命令层）、Open Quickly（fuzzy 排序/Ctrl+P 面板）
- [x] **Task 18 E2E 冒烟扩展（本任务）**：
  - `e2e/wdio.conf.ts` 预置 fixture（e2e/.fixtures/opening.md，配置加载时生成）+ capabilities args 追加 `--reopen-file=<fixture>`（Windows 反斜杠路径直传无 shell 转义）
  - 新建 `e2e/specs/document.e2e.ts`：两用例（启动参数打开渲染 h1「启动测试」+ 编辑输入自动保存写盘）
  - **E2E 实测 6/6 全绿**（document 2/2 + smoke 4/4，WebView2 151，双会话串行）：`--reopen-file` 启动链路（parse_cli_args → get_cli_args → resolveLaunch → openFile）在真实进程端到端打通；自动保存停笔防抖链路（markdownUpdated → 1s 防抖 → 写盘）实测落盘
  - smoke.e2e.ts 一处断言更新：初始文档标题由「欢迎使用 MarkWell」（01 默认文档，已随 02 空文档方案移除）改为 fixture 标题「启动测试」（E2E 恒以 --reopen-file 启动）
  - 全量门禁绿：vitest 45 files/394 tests、覆盖率全局 93.51%（阈值 ≥80）、02 核心服务全 100%、vue-tsc/eslint 0 问题、cargo test 40 通过、cargo clippy -D warnings 0 警告
- [x] **28/28 AC 覆盖表**（spec 定稿口径：F1:4、F11:4、F13:4、F14:4、F30:5、F31:5、C3:5）

| AC       | 说明（spec 原文语义）                    | 覆盖任务                               |
| -------- | ---------------------------------------- | -------------------------------------- |
| AC-F1-1  | 选择文件夹加载为侧栏当前目录             | Task 10/11                             |
| AC-F1-2  | 打开单文件父目录加载到侧栏               | Task 10/11                             |
| AC-F1-3  | 打开不存在路径（命令行传入）提示且不崩溃 | Task 13（--reopen-file 回退新建提示）  |
| AC-F1-4  | 侧栏菜单切换文件夹刷新文件树             | Task 10/11（目录切换服务层，UI 留 03） |
| AC-F11-1 | 按 Ctrl+P 弹出搜索面板                   | Task 17                                |
| AC-F11-2 | 输入片段模糊匹配当前文件夹文件并排序     | Task 17                                |
| AC-F11-3 | 选中结果按 Enter 打开该文件              | Task 17                                |
| AC-F11-4 | 无匹配显示空态提示不崩溃                 | Task 17                                |
| AC-F13-1 | 打开文件出现在最近列表首位               | Task 12                                |
| AC-F13-2 | 连续打开 12 个只保留最近 10 条           | Task 12                                |
| AC-F13-3 | Clear Items 清空但固定项保留             | Task 12                                |
| AC-F13-4 | 打开最近条目（不存在提示并移除）         | Task 12                                |
| AC-F14-1 | restore-folder 模式恢复上次文件夹        | Task 13/16                             |
| AC-F14-2 | restore-both 恢复文件夹并重开上次文件    | Task 13/16                             |
| AC-F14-3 | --new 覆盖设置恒新建                     | Task 13                                |
| AC-F14-4 | 恢复目标不存在回退新建并提示             | Task 13/16                             |
| AC-F30-1 | 停笔约 1 秒内容自动写盘                  | Task 14                                |
| AC-F30-2 | 持续编辑不停笔最长 5 分钟定时写盘        | Task 14                                |
| AC-F30-3 | 关闭自动保存开关仅手动 Ctrl+S 写盘       | Task 14                                |
| AC-F30-4 | 保存成功脏状态清除                       | Task 14                                |
| AC-F30-5 | 写盘失败提示错误且不标脏清除             | Task 14                                |
| AC-F31-1 | 异常退出后备份目录存在日期前缀草稿       | Task 15                                |
| AC-F31-2 | 未命名文档草稿名取首标题                 | Task 15                                |
| AC-F31-3 | 恢复入口列出可恢复草稿并可打开           | Task 15（列表数据源；UI 入口待 10/12） |
| AC-F31-4 | 正常退出草稿备份仍在                     | Task 15                                |
| AC-F31-5 | 草稿内容为空不产生空草稿                 | Task 15                                |
| AC-C3-1  | 打开 UTF-8 无 BOM 中文正常显示           | Task 2/5/9                             |
| AC-C3-2  | 打开 UTF-8 带 BOM 剥离且正常显示         | Task 2/5/9                             |
| AC-C3-3  | 打开 GBK 探测后正常显示（差异化增强）    | Task 2/5/9                             |
| AC-C3-4  | 保存落盘 UTF-8 无 BOM                    | Task 2/5/9                             |
| AC-C3-5  | CRLF 行尾可配；默认 LF                   | Task 2/5/9（行尾转换器收口）           |

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 02 模块收尾事项（本会话已完成主体）

- **Task 18 已提交**（test(02) 前缀，见「提交 hash 一节」）；模块 PR 待按 superpowers 流程双阶段 review 后合入 main（用户把关）
- 强制披露清单（PR 描述必列）：
  1. **UTC 草稿日期**：草稿名日期用 UTC（无 chrono 依赖；Windows 本地时区 ±1 天边缘，12 可换）
  2. **E2E 重启边界**：E2E 只覆盖「打开→编辑→自动保存落盘」；重启恢复链路由前端单测覆盖（resolveLaunch/草稿心跳），E2E 重启留 12 窗口外壳
  3. **自动保存默认开**：差异化于 Typora 默认关（数据安全优先，spec 待把关项按调研建议裁决）
  4. **watch_dir 待 03 消费**：目录监视命令（notify 递归）已注册，03 侧栏消费
  5. **FM CRLF 盲区**：单行 CRLF FM 的 eol 检测盲区由行尾转换器收口（Task 2/5/9 已覆盖回写路径）
  6. **固定项 UI 入口待 03/12**：最近文件固定项（pinned）服务层已支持，UI 入口延后
  7. **恢复入口按钮待 10/12**：Recover Unsaved Drafts 偏好面板按钮延后装配

### 2.2 跨模块接口增量（02 提供，03/10/12 消费）

- `editorManager.subscribeMarkdownUpdated(cb)`（Task 11 定义，Task 14/15/16 消费）：自动保存与草稿心跳的事件源；签名：`(cb: (markdown: string) => void) => () => void`（返回退订函数）
- Rust command 五个契约（Task 1/5/6/7/13）：`read_file`/`write_file`/`list_dir`/`watch_dir`（Channel 推送）/`get_cli_args` + 草稿三命令 `save_draft`/`list_drafts`/`recover_draft`
- 前端服务层：`DocumentSession`（openFile/openFolder/newDocument/save/saveAs/markDirty/markSaved/on）、`AutoSaveController`、`DraftRecovery`、`RecentFiles`、`resolveLaunch`、`OpenQuicklyPanel` + `searchQuickItems`/`buildQuickItems`（App.vue 装配）

### 2.3 03 侧栏/目录树模块（下一阶段）

- 目标：以 02 已交付的 list_dir/watch_dir/DocumentSession.currentDir 为数据源实现侧栏文件树；02 遗留项（固定项 UI 入口、watch_dir 消费）随 03/12 落地
- 当前状态：**未开始**；`docs/specs/modules/03-侧边栏与目录树.md` 已定稿（如存在）
- 验证方式: 03 计划覆盖 spec 全部 AC 无占位符；执行后门禁全绿 + 用户把关

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: Windows 11 10.0.26200；Node 24 + npm 11（npm 有 allow-scripts 安全策略）；Rust stable-x86_64-pc-windows-msvc（VS 2022）；WebView2 本机 151.0.4129.86；pre-commit framework（Python 3.14.6 @ D:\code\envs\python\3.14.6）；vitest 4.1.10（jsdom 30）；WebdriverIO 9.30 + tauri-driver（cargo 安装，非 npm 包——`npx tauri-driver` 会 404，须用 `$HOME/.cargo/bin/tauri-driver`）+ e2e/.driver/msedgedriver.exe（本地驱动）
- 关键配置: Bash 会话需 `export PATH="$HOME/.cargo/bin:$PATH"`；Git Bash 传 Windows 开关用 `//`；pre-commit + pre-push 钩子已安装（内存教训配置生效：vitest maxWorkers 4、重量级钩子 pass_filenames: false）
- E2E 运行流程（本地三终端，实测可用）:
  ```bash
  # 终端 1：Vite dev server（README 规范流程；tauri-driver 负责拉起应用二进制）
  npm run dev
  # 终端 2：WebDriver 服务（注意：tauri-driver 是 cargo 二进制，不是 npm 包）
  tauri-driver --native-driver e2e/.driver/msedgedriver.exe
  # 终端 3：
  npm run test:e2e
  ```
  前置：`cargo build`（src-tauri）产出最新 debug 二进制（E2E 前必须重新编译，二进制易过期）
- 安装/构建/启动命令:
  ```bash
  cd D:\code\project\typora
  npm test                    # 45 files / 394 tests，峰值内存受 maxWorkers 4 限流
  npm run test:coverage       # 全局 93.51%（阈值 ≥80）、02 核心服务 100%
  npm run typecheck && npm run lint
  cargo test --manifest-path src-tauri/Cargo.toml   # 40 通过
  cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
  npm run test:e2e            # 本地 WebDriver E2E（需终端 1/2 先行）
  ```

## 4. 全局约定 (Locked Conventions)

- **spec 驱动流程**: brainstorm → spec → writing-plans → TDD → 执行 → review（superpowers 技能）；02 spec 已定稿（docs/specs/modules/02-文档管理.md），改 spec 必须先经用户同意
- **clean-room 边界**: 仅限 Typora 官方公开文档 + 用户实操观察 + 开源生态；禁止解包 Typora 闭源代码；内置主题原创
- **注释/编码规范**: 全部注释/日志/文档中文；UTF-8 无 BOM、LF；核心功能单测 100%、非核心 ≥80%（vite.config.ts thresholds 已配）；死代码零容忍（本次改动产生的废弃代码同次提交清理）；提交前过 pre-commit（prettier）
- **AC 覆盖率口径**: 02 模块 **28/28 AC**（spec 第 145 行明文：F1:4、F11:4、F13:4、F14:4、F30:5、F31:5、C3:5）
- **CI 结构**: PR → Quality Gate（ESLint/Prettier/vue-tsc/Vitest + rustfmt/clippy/cargo test + gitleaks/npm audit/cargo-deny）→ Build Verification；CI 冒烟为进程级确定性验证（e2e/smoke-ci.mjs），**不得**改回 CDP 调试端口方案
- **SDD ledger**: `.superpowers/sdd/2026-08-15-02-文档管理/progress.md` 是 02 模块恢复地图（事实校正/裁决/deferred 全量记录）；新会话续接 02 收尾必须读它；03 模块会建自己的 ledger
- **跨模块接口契约（02 提供，03/10/12 消费，签名锁定）**: DocumentSession 方法集、subscribeMarkdownUpdated、resolveLaunch、RecentFiles、AutoSaveController/DraftRecovery、Rust 九命令 + 草稿三命令；01 提供的 editorManager 方法集/三事件桥/revealRange/setUploadHandler/openExternalUrl/addEditorKeymap/text 序列化处理器签名锁定，只消费不修改
- **内存安全红线（勿再犯）**: vitest 并发上限 maxWorkers 4（vitest 4 已移除 poolOptions.forks.maxForks）；重量级钩子（clippy/cargo-test/vitest）必须 pass_filenames: false 整段一次；跑测试/推送前检查残留 vitest 进程（甄别 CommandLine，严禁误杀其他项目的 node 进程）

## 提交 hash 一节（Task 18 提交记录）

- Task 18 提交：**b43fe1b**（4 files changed, 174 insertions(+), 3 deletions(-)；wdio.conf.ts + document.e2e.ts + smoke.e2e.ts 断言更新 + 本进度文档）
- 分支：feat/02-document-management（22 个提交，main..HEAD）

## 2.x 最终 review + 修复波 + PR（2026-08-15 晚补充）

- **最终全分支 review**（最强模型，4ba6206..0f3cc83）：结论 **Ready to merge: With fixes**——2 项 Important 产品链路缺陷（单测未覆盖的集成缺口），已由最终修复波闭环：
  - **I-1** App.vue 用 `readFile` 探测文件夹存在性恒 false（Rust read_file 对目录必失败）→ restore-folder/restore-both/custom-folder 正常路径全失效、每次启动回退新建并误提示（AC-F14-1/2 判 ❌）→ 修复 `8f640d9`：file-io.ts 新增 `probePathExists`（listDir 优先/readFile 兜底）+ 3 分支专测 + App.vue 接线
  - **I-2** `RecentFiles.record()` 无生产调用点 → 最近文件列表恒空（AC-F13-1/2 产品链路不可达）→ 修复 `89937b0`：document-session openFile/openFolder/saveAs 接线 recordRecent（失败静默；saveAs 写盘成功后才记录）；顺带修正 document-session.spec 既有 mock 缺陷（mockUpdateSettings 缺 mockResolvedValue 致接线断言不可达）
  - 修复波复审：**全 ADDRESSED，无新破坏**；AC 核对 ✅18 / ⚠️7（模块边界延后项：File 菜单/侧栏/恢复按钮/固定项入口等，已披露）/ ❌3（随 2 项修复闭环）
- **PR #2 已创建**（2026-08-15，https://github.com/mwrforever/typora-replica/pull/2）：描述含 AC 通过清单 28/28 + 覆盖率报告 + 强制披露清单 8 项（自动保存默认开差异化/E2E 重启边界/UTC 草稿日期/FM 盲区收口/尾换行口径/跨模块契约/遗留 minor/模块边界延后项）；**合入前用户把关**
- 收尾提交：`bbfdf5f`（docs: 实现计划入库 + e2e/.fixtures 入 gitignore）；分支共 24 个提交（main..HEAD），全部门禁绿（vitest 399 + cargo 40）
- 遗留（非阻塞）：`skills/` 未跟踪目录（code-review-promax 技能，01 遗留，与 git 无关可随时删）；watch.rs 注释 rename 误导（03 消费前顺手修）；最近文件固定项优先截断策略留待 03 侧栏落地时用户拍板
