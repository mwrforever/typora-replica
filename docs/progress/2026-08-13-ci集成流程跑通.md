# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)

- workspace_dir: D:\code\project\typora
- saved_at: 2026-08-13T20:45:10+08:00
- 版本控制提交号: a8eaed910ee56e88f853f2fc726294a73e353cbc
- 任务一句话描述: 跑通项目的 GitHub Actions CI 集成流程（Quality Gate + Build Verification 全绿）

## 1. 已完成 (Completed) — 简要标记

- [x] CI 冒烟失败根因定位：CI runner 镜像自带 WebView2 150.0.4078.105 存在微软上游回归——提权进程下 CDP 调试端口静默不监听（WebView2Feedback#5639），与项目代码无关
- [x] 全部 CDP 可行路径排查完毕（9 轮 CI + 本地对照实验）：env 注入/固定端口/runas/schtasks/新用户/固定版本 CAB/注册表策略/psexec 均不可行，结论见 e2e/README.md
- [x] CI 冒烟重写为确定性验证（e2e/smoke-ci.mjs）：应用进程存活 + WebView2 初始化 + 前端由 WebView2 真实加载（netstat ESTABLISHED 证据），本地与 CI 均通过
- [x] Quality Gate 全绿：前端 ESLint/Prettier/vue-tsc/Vitest + Rust rustfmt/clippy/cargo test + gitleaks/npm audit/cargo-deny
- [x] Build Verification 全绿：Windows 构建（Tauri release 安装包）+ CI 冒烟 + 产物归档
- [x] 修复流程级 bug：workflow 中 `set -e` 导致失败诊断块为死代码；Git Bash `expand` 是 coreutils 需用 `"$WINDIR\System32\expand.exe"`；YAML heredoc 缩进语法错误
- [x] 项目记忆已更新：`C:\Users\Lenovo\.zcode\cli\memories\projects\typora-cd575599c16581af\memory\windows-env-gotchas.md`（CI 大坑 + 三个环境坑）、`project-overview.md`（B3 阶段完成）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 C 阶段调研汇总 → 全量功能调研 spec

- 目标: 产出 `docs/specs/00-typora-full-feature-research.md` 全量功能调研 spec，经用户校准后拆分为 12 个模块 spec
- 当前状态: 3 个调研子代理已派发（编辑语法域/文件导航域/主题导出设置域），本会话未跟踪其产出；docs/specs/ 目录已存在
- 涉及文件/产物:
  - `D:\code\project\typora\docs\specs\` [待创建] — 全量 spec 与后续模块 spec 的存放目录
- 已锁定决策: spec 驱动开发流程（brainstorm → spec → writing-plans → TDD → 执行 → review，superpowers 技能）；clean-room 边界（调研只允许：Typora 官方公开文档、用户实操体验、开源生态与公开标准，禁止解包/提取 Typora 闭源代码）
- 阻塞点/风险: 调研子代理的结果需要先确认是否已返回、质量是否符合 clean-room 边界
- **下一步具体动作**: 先查看 docs/specs/ 与项目记忆中是否有子代理返回的调研产出；若无，检查子代理任务状态并重新派发/收取 3 个域的调研结果
- 验证方式: 三个域（编辑语法/文件导航/主题导出设置）均有成文调研材料，且来源全部符合 clean-room 边界三条

### 2.2 用户校准全量 spec

- 目标: 用户逐项确认全量 spec 的功能范围与优先级，形成 12 模块拆分定稿
- 当前状态: 未开始，阻塞在 2.1
- 涉及文件/产物:
  - `D:\code\project\typora\docs\specs\00-typora-full-feature-research.md` [待创建] — 全量功能调研 spec
- 已锁定决策: 12 个模块：编辑核心/文档管理/文件树/多标签/大纲/搜索替换/图片粘贴存盘/主题/导入导出/设置快捷键/状态栏/窗口外壳
- 阻塞点/风险: 无，纯流程依赖
- **下一步具体动作**: 待 2.1 完成后，将全量 spec 呈现给用户，逐项收集校准意见
- 验证方式: 用户明确确认功能范围无遗漏/无多余，12 模块边界清晰

### 2.3 模块 spec 拆分（NN-*.md）

- 目标: 在 docs/specs/modules/ 下产出 12 份精细化模块 spec（NN-*.md）
- 当前状态: 未开始，阻塞在 2.2
- 涉及文件/产物:
  - `D:\code\project\typora\docs\specs\modules\` [待创建] — 模块 spec 目录
- 已锁定决策: 每模块独立 PR；CI Quality Gate 全绿才可合入
- 阻塞点/风险: 无，纯流程依赖
- **下一步具体动作**: 待 2.2 校准后，按 12 模块顺序逐份撰写模块 spec
- 验证方式: 每份 spec 通过用户审核后进入 E 阶段实现

### 2.4 E 阶段逐模块实现

- 目标: 12 个模块全部实现并通过 CI（Quality Gate + Build Verification）
- 当前状态: 未开始（脚手架阶段完成，src/ 仍是 Tauri+Vue 默认脚手架，仅 greet 命令）
- 涉及文件/产物:
  - `D:\code\project\typora\src\` [修改] — Vue 3 + TS + Pinia + Milkdown v7 Crepe（WYSIWYG）前端
  - `D:\code\project\typora\src-tauri\src\` [修改] — Rust 侧（notify/walkdir/regex + tauri-plugin-dialog/fs/store/opener）
- 已锁定决策: 技术栈（Tauri 2 + Vue 3 + TS + Vite + Pinia + Milkdown Crepe WYSIWYG + KaTeX + Mermaid + GFM）；每模块独立 PR；CI 全绿才能合入
- 阻塞点/风险: 依赖 2.1-2.3 的 spec 产出
- **下一步具体动作**: 待模块 spec 定稿后，按 superpowers 流程逐模块实现（writing-plans → TDD → 执行 → review）
- 验证方式: 每模块 PR 的 Quality Gate 与 Build Verification 均绿

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: Windows 11 10.0.26200（本机）/ Windows Server 2025（CI）；Node 24 + npm 11（npm 有 allow-scripts 安全策略）；Rust stable-x86_64-pc-windows-msvc（VS 2022 已装）；WebView2 本机 151.0.4129.78 / CI 镜像 150.0.4078.105
- 关键配置: Bash 会话需 `export PATH="$HOME/.cargo/bin:$PATH"` 才有 cargo；无其他必需环境变量
- 安装/构建/启动命令:
  ```bash
  cd D:\code\project\typora
  npm ci                          # 前端依赖（注意 npm approve-scripts 放行 postinstall）
  npm run dev                     # 前端 dev server（1420）
  cargo build --manifest-path src-tauri/Cargo.toml   # debug 二进制
  npm test                        # Vitest
  npx tauri build                 # Windows release 安装包
  npm run test:e2e                # 完整 CDP/WebDriver E2E（仅本地，见 e2e/README.md）
  ```

## 4. 全局约定 (Locked Conventions)

- CI 结构：PR → Quality Gate（前端 + Rust + 安全）→ Build Verification（Windows 构建 + CI 冒烟 + 产物）；Release tag 触发自签名 + sha256 + GitHub Release
- CI 冒烟为确定性验证（e2e/smoke-ci.mjs），**不得**改回依赖 CDP 调试端口的方案，除非微软修复 WebView2 150 回归或 runner 镜像升级（届时见 e2e/README.md 恢复指引）
- clean-room 边界：绝不逆向/解包 Typora 闭源代码，调研仅限官方公开文档 + 用户实操描述 + 开源生态/公开标准
- 全部注释/日志/文档必须中文；源码 UTF-8 无 BOM、LF 行尾；核心功能单测 100%、非核心 ≥80%
- 死代码零容忍：本次改动产生的废弃代码必须在同次提交清理；历史遗留死代码只指出不擅删
- Git Bash 环境坑（详见项目记忆 windows-env-gotchas.md）：`expand` 用 `"$WINDIR\System32\expand.exe"`；`reg add` 开关写 `//v //t //d //f`；YAML 块标量内 heredoc 的 EOF 必须在块基准缩进上；pre-commit 重量级钩子用 `bash -c '...'` 包裹
