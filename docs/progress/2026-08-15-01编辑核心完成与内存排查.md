# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)

- workspace_dir: D:\code\project\typora
- saved_at: 2026-08-15T14:35:00+08:00
- 版本控制提交号: e3142d8（分支 feat/01-editor-core；main 仍在 9c90e0e）
- 任务一句话描述: 01 编辑核心模块实现完毕，内存问题已根治（两层根因实验闭环），2026-08-15 审查 12 项 FIX 已全部修复并回归（291 用例全绿），分支已推送 origin、PR #1 已创建（OPEN，含修复提交），等待用户 review 合入 main 后继续 02 模块；下一步为项目整体 UI 设计稿（HTML）落地

## 1. 已完成 (Completed) — 简要标记

- [x] MarkWell 设计系统生成（ui-ux-pro-max）：`design-system/markwell/MASTER.md` + `pages/editor.md`（桌面离线字体栈、Crepe 变量映射、亮/暗两态色板）
- [x] 01 编辑核心实现计划：`docs/superpowers/plans/2026-08-14-01-编辑核心.md`（22 任务、80 条 AC 映射、无占位符）
- [x] **01 模块全部 22 个任务实现并通过任务级审查**（分支 feat/01-editor-core，40+ 提交）：编辑器基座/设计令牌/EditorPage、实例管理与事件桥（markdownUpdated 300ms/updated 200ms/selectionUpdated 即时）、revealRange、keymap 注册表（Ctrl+[/] 反向缩进、Ctrl+1~6/0/=/- 标题、Ctrl+Shift+K/`、表格 Tab 加行）、输入规则注册表（硬换行、>> 嵌套引用、setext H2、宽松 ATX 标题、Typora 建表、Pandoc 行内数学）、Front Matter 剥离/回写、[toc] 节点+NodeView、Ctrl+点击开链接、onUpload 注册表、DOMPurify 安全清洗（sandbox 恒归一化）、Mermaid renderPreview 懒加载+legacy 转换+右键菜单、GFM 下划线序列化（flanking 感知）、E2E 冒烟三场景
- [x] 全量门禁绿：267 单测全过、覆盖率全局 99.85%/核心 100%、typecheck/lint/cargo test 全过、本地 E2E 4/4（WebView2 151）
- [x] 最终全分支 review（最强模型）：Ready to merge: With fixes → 3 项 Important 已由最终修复波闭环（mermaid 唯一渲染 id/哨兵碰撞防护/`-.->` 虚线保护），复审全 ADDRESSED
- [x] E8 三条 AC 用户裁决已回写 spec（9491b3d）：对齐=GFM 标记、单列表 Tab=下移、E8-6 单测口径=结构断言
- [x] 内存问题初步修复（已提交 2cdc4fd）：vitest 限流 `pool: 'forks' + maxForks: 4` → npm test 峰值 4.18GB、coverage 峰值 4.81GB（实测），267/267 仍全绿
- [x] SDD ledger：`.superpowers/sdd/2026-08-14-01-编辑核心/progress.md`（22 任务完整记录：事实校正/用户裁决/deferred/parked 项，新会话必须读）
- [x] **2026-08-15 审查清单 12 项 FIX 全部修复**（提交 e3142d8，23 文件 +475/-55；修复记录全文见 `docs/bugs/2026-08-15-修复清单.md` 第六节）：FIX-1 硬换行补行尾判定、FIX-2 测试基座 async destroy + root.remove、FIX-3/4/8 code mark 守卫族（`isInInlineCode` 查前后节点 marks——**不能用 `$from.marks()`**，inlineCode `inclusive:false` 致跨度末尾守卫静默失效，实测踩坑）、FIX-5 硬换行保护还原限定后随 break 节点（段尾两空格以「字面空格+`&#x20;`」组合往返，两空格完整保留且定点）、FIX-6 链接协议白名单 + 纯 Web fallback + catch、FIX-7 mermaid 渲染代次守卫、FIX-9 FM 定界符 CRLF 归一、FIX-10 destroy 关闭右键菜单、FIX-11 空 FM 识别（二分支正则，闭合仍须独立成行）、FIX-12 SVG 下载延迟 revoke
- [x] 回归基线（修复后实测）：**全量 vitest 291 用例全绿**（基线 267 + 新增 24）、覆盖率阈值通过（editor 模块 100%、全局 99.86%）、vue-tsc/eslint/pre-commit validate 全过

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 内存问题根治 + 推送分支 + 创建 PR（✅ 已完成 2026-08-15）

- 结果: `feat/01-editor-core` 已推送 origin（66 commits），**PR #1 已创建**（https://github.com/mwrforever/typora-replica/pull/1，OPEN，描述含强制披露清单 + 内存根治说明）
- **内存问题根因链（两层，均有实验证据）**:
  1. **第一层（2cdc4fd 的限流从未生效）**: `poolOptions.forks.maxForks: 4` 在 vitest 4.1.10 已被移除（官方 DEPRECATED 警告 + 类型定义确认顶层改为 maxWorkers），旧配置静默失效 → 实测按 24 核全并发拉起 24 worker（24 核 CPU，workers=24 采样实证），总内存 20GB+ → 两次推送 99% 的第一层根因；"限流后实测 4.18GB"是采样窗口错过峰值（24 并发下 267 用例仅 19.59s 墙钟）的假象
  2. **第二层（pre-commit xargs 分块并行）**: pre-commit 框架按 CPU 核数把整段推送 range 的文件分块、ThreadPoolExecutor 并行跑钩子实例（xargs.py: max_args=max(4, ceil(N/24))，源码已核）→ 推送时 vitest 钩子被并行拉起约 10 组 npm test（采样实证 15 vt 主进程 + 37 worker ≈ 13GB）+ eslint（无 stages 默认继承全部 stage）并行 30 实例（≈3GB），叠加 cargo 编译后推送峰值 **15.9GB、可用内存最低 3.5GB**；这也推翻了上一会话的"孤儿 worker"理论——实验证明杀 vitest 主进程后 worker 3 秒内自动退出（IPC 断开检测），真实机制是进程树不被递归终止（杀 npx 层后 vitest 树继续跑完）+ xargs 并行
- 修复（两个提交）:
  - `1ac8c56`: vite.config.ts 删除无效 poolOptions，改用顶层 `maxWorkers: 4` + `execArgv: ["--max-old-space-size=2048"]` 堆上限兜底，注释修正
  - `5b341a9`: .pre-commit-config.yaml 重量级钩子（clippy/cargo-test/vitest）加 `pass_filenames: false` 整段只执行一次；轻量钩子（eslint/prettier/rustfmt）显式 `stages: [pre-commit]`
- 修复后实测（全有采样记录）: npm test 267/267 峰值 <1GB（vitestProcs 恒 ≤5）、coverage 核心 100% 峰值 <1GB、32 文件模拟整段 range 的 pre-push 仅 1 组 npm test（1 vt + 4 wk）、真实推送全程可用内存不低于 12.9GB、零残留进程
- 门禁: typecheck/lint/cargo test 全过；pre-commit + pre-push 钩子全 Passed
- 遗留: gh CLI keyring token 失效（git 凭据可用，`GH_TOKEN=$(git credential fill ...)` 方式可替代，或用户自行 `gh auth refresh`）

### 2.2 用户 review PR 后合入 main（当前阻塞点）

- 目标: 用户 review 通过后合入 main，01 模块正式收官
- 当前状态: **PR #1 OPEN 待 review**（https://github.com/mwrforever/typora-replica/pull/1，67 commits；PR 描述含 AC 通过清单 79/80 + 六项强制披露 + 内存根治说明；最新提交 e3142d8 为 12 项 FIX 修复，review 时一并确认）
- 已锁定决策: 每模块一个 PR 合入主干（项目惯例）；合入前用户把关（向用户展示 PR 摘要：AC 通过清单 + 覆盖率报告）；合入走 GitHub PR merge（或 `git merge --no-ff`）
- **下一步具体动作**: 向用户展示 PR #1 摘要（AC 79/80 + 覆盖率 99.86%/核心 100% + 披露清单 + 12 项 FIX 修复说明），等待 review 意见；通过后合入并删除 feat/01-editor-core 分支
- 验证方式: main 上出现 01 模块全部提交，CI Quality Gate 全绿

### 2.3 02 文档管理模块（下一模块）

- 目标: 按 01 同款流程（writing-plans → SDD 逐任务 → 用户把关）实现 02 模块
- 当前状态: 未开始（01 的 `docs/specs/modules/02-文档管理.md` 已定稿）
- 已锁定决策（**02 开工前设计输入**，最终 review 已明确列出）:
  1. 事件订阅 API 形态：01 交付的是 `attachEditorEvents(crepe, callbacks)` + 全局 `detachEditorEvents()`（无单订阅解绑），`setupEditorEvents` 为空桩（TODO(02) 标注）——02 需在 editorManager 上补透传或明确消费模式
  2. markdownUpdated 全链路延迟 = 本层 300ms + plugin-listener 内置 200ms = **500ms**（02 自动保存定时器设计须知晓）
  3. 尾随换行策略：editor-manager.getMarkdown() 剥**全部**尾随换行（测试助手只剥一个）——02 落盘时定夺
  4. FM CRLF：01 已修复定界符随内文归一（FIX-9，CRLF 文档定界符不再混行）；正文行尾统一换行归 02 行尾转换器收口；合法 YAML 误拒（撇号/块标量）失败方向安全，如需完整 YAML 另立任务引入 js-yaml
  5. 01 已暴露的跨模块接口（勿改签名）：create/destroy/getCrepe/getEditor/getMarkdown/setReadonly/setDocumentTransformers/adopt、onMarkdownUpdated 事件桥、revealRange(from,to)、setUploadHandler/getUploadHandler、openExternalUrl（12 可替换，现含协议白名单 + 纯 Web fallback）
- **下一步具体动作**: 01 合入 main 后，以 `docs/specs/modules/02-文档管理.md` 为输入走 writing-plans（产出 `docs/superpowers/plans/2026-08-15-02-文档管理.md`），然后 SDD 流程（sdd-workspace → task-brief → 逐任务实现 + 双阶段 review）
- 验证方式: 02 计划覆盖 spec 全部 AC 无占位符；执行后门禁全绿 + 用户把关

### 2.4 项目整体 UI 设计稿（HTML）落地（✅ 已完成 2026-08-15，提交 64d46c9）

- 结果: `design-system/markwell/pages/app-shell.html`（单文件自包含 1293 行）——窗口外壳（7 菜单栏）+ 侧栏三面板（文件树/大纲/搜索，Ctrl+Shift+1/2/3 语义）+ 标签条 + 800px 居中纸面 + 状态栏（字数/侧栏开关）+ 基础渲染展示（标题/代码围栏/GFM 表格/任务列表/嵌套引用/KaTeX 公式/Mermaid 令牌化 SVG/toc）+ 设计令牌附页（10 色板实时取值）；亮/暗双态经 `.markwell-dark` 切换（与 editor.md 映射一致）；playwright 实测结构/交互/控制台零错误
- 已锁定决策: 设计稿为**界面基准**，后续模块（03/04/05/06/11/12）按此落地；交互仅演示语义，产品侧由 08 主题/12 外壳模块接管；设计令牌以 MASTER.md + editor.md 为准
- **下一步具体动作**: 02 文档管理模块开工前，向用户展示设计稿（浏览器打开 `design-system/markwell/pages/app-shell.html`）确认界面风格；确认后按 02.3 流程推进 02 模块

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: Windows 11 10.0.26200；Node 24 + npm 11（npm 有 allow-scripts 安全策略）；Rust stable-x86_64-pc-windows-msvc（VS 2022）；WebView2 本机 151.0.4129.78；pre-commit framework（Python 3.14.6 @ D:\code\envs\python\3.14.6）；vitest 4.1.10（jsdom 30）
- 关键配置: Bash 会话需 `export PATH="$HOME/.cargo/bin:$PATH"`；Git Bash 传 Windows 开关用 `//`（如 `taskkill //PID`）；pre-commit 钩子已安装（pre-commit + pre-push）
- 安装/构建/启动命令:
  ```bash
  cd D:\code\project\typora
  npm test                    # 267 用例，峰值 ~4.2GB（限流后）
  npm run test:coverage       # 全局 99.85%、核心 100%
  npm run typecheck && npm run lint
  cargo test --manifest-path src-tauri/Cargo.toml
  npm run test:e2e            # 本地 WebDriver E2E（tauri-driver，需先 npx tauri dev）
  ```

## 4. 全局约定 (Locked Conventions)

- **spec 驱动流程**: brainstorm → spec → writing-plans → TDD → 执行 → review（superpowers 技能）；01 spec 已定稿，改 spec 必须先经用户同意（E8 三条裁决已回写 9491b3d）
- **clean-room 边界**: 仅限 Typora 官方公开文档 + 用户实操观察 + 开源生态；禁止解包 Typora 闭源代码；内置主题原创
- **注释/编码规范**: 全部注释/日志/文档中文；UTF-8 无 BOM、LF；核心功能单测 100%、非核心 ≥80%（vite.config.ts thresholds 已配）；死代码零容忍（本次改动产生的废弃代码同次提交清理）；提交前过 pre-commit（prettier）
- **AC 覆盖率口径（重要）**: 01 实际 **79/80**——AC-E21-2（图表主题变量跟随）依赖 08 主题模块 + 偏好设置（Diagrams 开关），显式延后至 08 覆盖。**任何文档/PR 中不得再写「80/80 全覆盖」**（计划自审表原文 80/80 已被 Task 22 审查纠正）
- **CI 结构**: PR → Quality Gate（ESLint/Prettier/vue-tsc/Vitest + rustfmt/clippy/cargo test + gitleaks/npm audit/cargo-deny）→ Build Verification；CI 冒烟为进程级确定性验证（e2e/smoke-ci.mjs），**不得**改回 CDP 调试端口方案
- **SDD ledger**: `.superpowers/sdd/2026-08-14-01-编辑核心/progress.md` 是 01 模块的恢复地图（事实校正/裁决/deferred 全量记录）；新会话续接 01 收尾必须读它；02 模块会建自己的 ledger（sdd-workspace 脚本自动生成目录）
- **跨模块接口契约（01 提供，02/05/06/07/10/11 消费，签名锁定）**: editorManager 单例方法集 + adopt、事件桥三事件（markdownUpdated 300ms+内置 200ms/updated 200ms+内置 200ms/selectionUpdated 即时）、revealRange(from,to)、setUploadHandler/getUploadHandler（07 注入点，缺省 blob URL）、openExternalUrl（12 可替换）、keymap 注册表 addEditorKeymap（10 扩展）、text 序列化处理器（GFM 下划线+硬换行保护，02 落盘链路不得绕过）
- **PR 摘要强制披露清单**（创建 PR 时必须全部列出）:
  1. 79/80 AC（E21-2 延后 08，附理由）
  2. E19 行内数学五项停靠：数字回退仅逐键输入触发（粘贴/IME/多字符/选区替换不触发）、瞬时 KaTeX 闪烁、回删后死字面 `$x$`、宽正则吞叶子占位符、双反斜杠奇偶判定
  3. FM 延后项：CRLF 归一化归 02；合法 YAML 误拒（安全失败方向）
  4. E21 延后项：图表主题变量跟随（即 AC-E21-2）、右键菜单暗色主题回退亮色（08 处理）
  5. 序列化口径变更（产品级）：GFM 单词内下划线不转义、代码块语言小写归一、表格对齐 `:-:` 标记、`\$x\$2` 落盘（Typora 直写 `$x$2` 重开会渲染——解析侧既有缺口）
  6. 版本事实：`|2x2|`+Enter 现会建成表头 "2x2" 的表（Typora 语义倾向）；标题内建表窄边界
- **已知残余 minor（ledger 有全量清单，最终 review 判定不阻塞合入）**: 哨兵 U+E001 移位碰撞（私有区字符，无现实回归）；`-.->` 已修复；菜单视口边缘翻转等（openExternalUrl void rejection 已由 FIX-6 修复移除；mermaid 渲染竞态已由 FIX-7 修复）
